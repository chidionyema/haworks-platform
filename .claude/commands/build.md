Build a service using its solution filter. Fast (no analyzers).

Usage: /build identity

Run: `dotnet build filters/$ARGUMENTS.slnf -v q --nologo`

If no argument: `dotnet build HaworksPlatform.sln -v q --nologo`
