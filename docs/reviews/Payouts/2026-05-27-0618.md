=== Continuous Review: Payouts (2026-05-27T06:18:25Z) ===
Files to review: 36

I'll conduct a comprehensive code review of the Payouts service, examining every aspect from security vulnerabilities to domain logic correctness.

## CRITICAL: Commission calculation inconsistency in demo endpoint

### CRITICAL: Demo commission calculation doesn't match production logic
- **File**: src/Payouts/Payouts.Api/Controllers/LedgerController.cs:61
- **Issue**: Demo uses `paymentCents / 10` but credits full amount then debits commission, while production LedgerService calculates `amountCents * commissionRate / 100m` and credits only net amount
- **Impact**: Demo shows incorrect double-entry accounting, misleading stakeholders about actual business logic
- **Fix**: Align demo logic with production: `var commissionCents = paymentCents * 10 / 100; var sellerAmountCents = paymentCents - commissionCents;` and credit only net amount

### CRITICAL: Non-idempotent payout retries
- **File**: src/Payouts/Payouts.Application/Disbursements/Services/DisbursementService.cs:87
- **Issue**: `Guid.NewGuid()` generates new idempotency key on every retry, allowing duplicate payouts
- **Impact**: Job failures could result in double payments to sellers, causing financial loss
- **Fix**: Generate idempotency key deterministically: `var idempotencyKey = $"PAYOUT:{sellerId}:{account.BalanceCents}:{DateTime.UtcNow:yyyyMMdd}";`

### CRITICAL: Deadlock risk in parallel payout processing
- **File**: src/Payouts/Payouts.Application/Disbursements/Services/DisbursementService.cs:107
- **Issue**: `Parallel.ForEachAsync` with database transactions and `FOR UPDATE` locks can cause deadlocks when payouts access accounts in different orders
- **Impact**: Service becomes unstable under load, payout processing fails
- **Fix**: Process sequentially or implement proper deadlock retry logic with exponential backoff

### HIGH: Inconsistent seller account lookup in refunds
- **File**: src/Payouts/Application/Ledger/Services/LedgerService.cs:163
- **Issue**: Query lacks `OrderBy`, could return different accounts if seller has entries in both Pending and Payable for same reference
- **Impact**: Refunds might be applied to wrong account type, corrupting ledger state
- **Fix**: Add `.OrderBy(x => x.Account.Type)` for deterministic results

### HIGH: Oversimplified Stripe status mapping
- **File**: src/Payouts/Payouts.Infrastructure/Gateways/StripePayoutGateway.cs:67
- **Issue**: Maps all non-reversed transfers to `InTransit`, ignoring Stripe's actual status (pending, paid, failed, canceled)
- **Impact**: Payout status doesn't reflect reality, manual intervention required for failed transfers
- **Fix**: Implement proper status mapping based on Stripe's actual transfer states and webhook events

### HIGH: Missing long overflow protection
- **File**: src/Payouts/Payouts.Domain/Aggregates/LedgerAccount.cs:17
- **Issue**: No check for overflow when adding amounts to `BalanceCents`
- **Impact**: In high-volume systems, balance could overflow and become negative, corrupting financial data
- **Fix**: Add overflow check: `if (BalanceCents > long.MaxValue - amountCents) throw new OverflowException();`

### HIGH: Unbounded query in disbursement processing
- **File**: src/Payouts/Payouts.Application/Disbursements/Services/DisbursementService.cs:38
- **Issue**: Loads all eligible accounts (limited to 500) without considering memory usage or processing time
- **Impact**: Service degradation with large datasets, potential memory exhaustion
- **Fix**: Implement cursor-based pagination: `WHERE Id > @lastProcessedId ORDER BY Id LIMIT @batchSize`

### MEDIUM: Missing input validation in consumers
- **File**: src/Payouts/Infrastructure/Messaging/Consumers/PaymentCompletedConsumer.cs:29
- **Issue**: No validation that `AmountCents > 0` or that currency is valid 3-letter code
- **Impact**: Invalid events could corrupt ledger with zero or negative amounts
- **Fix**: Add validation: `if (evt.AmountCents <= 0 || string.IsNullOrEmpty(evt.Currency) || evt.Currency.Length != 3) return;`

### MEDIUM: Race condition in seller registration
- **File**: src/Payouts/Application/Sellers/Commands/RegisterSeller/RegisterSellerCommand.cs:47
- **Issue**: Window between Stripe account creation and database save where orphaned Stripe accounts could accumulate
- **Impact**: Failed registrations leave cleanup burden and potential Stripe API rate limiting
- **Fix**: Create Stripe account inside database transaction using saga pattern or implement cleanup job

### MEDIUM: Information disclosure in error messages
- **File**: src/Payouts/Payouts.Domain/Aggregates/LedgerAccount.cs:25
- **Issue**: Error exposes internal account ID and exact balance amounts
- **Impact**: Sensitive financial data could leak to logs or API responses
- **Fix**: Use generic error: `throw new InvalidOperationException("Insufficient account balance for this operation");`

### MEDIUM: Missing database constraints for data integrity
- **File**: src/Payouts/Payouts.Infrastructure/Persistence/PayoutsDbContext.cs
- **Issue**: No check constraints for positive balances, valid currency codes, or foreign key relationships
- **Impact**: Invalid data could be inserted, bypassing application-level validation
- **Fix**: Add check constraints: `CHECK ("BalanceCents" >= 0)`, `CHECK (LENGTH("Currency") = 3)`

### MEDIUM: N+1 query pattern in disbursement service
- **File**: src/Payouts/Payouts.Application/Disbursements/Services/DisbursementService.cs:43-47
- **Issue**: Loads accounts then profiles separately instead of using joins
- **Impact**: Performance degradation with multiple database round trips
- **Fix**: Use join: `FROM LedgerAccounts a INNER JOIN SellerProfiles p ON a.OwnerId = p.SellerId`

### LOW: Missing IPv6 support in URL validation
- **File**: src/Payouts/Payouts.Api/Controllers/SellersController.cs:50
- **Issue**: SSRF protection only checks IPv4 private ranges, ignores IPv6 link-local/private ranges
- **Impact**: Potential SSRF via IPv6 addresses
- **Fix**: Add IPv6 validation for `fc00::/7`, `fe80::/10`, `::1/128`

### LOW: Hardcoded system platform ID
- **File**: src/Payouts/Application/Ledger/Services/LedgerService.cs:19
- **Issue**: Platform ID hardcoded as magic GUID
- **Impact**: Difficult to change, not documented as system constant
- **Fix**: Move to configuration: `services.Configure<LedgerOptions>(config => config.PlatformAccountId = ...)`

### LOW: String enum storage inefficiency
- **File**: src/Payouts/Payouts.Infrastructure/Persistence/PayoutsDbContext.cs:42
- **Issue**: Storing enum as string uses more space and creates migration issues if enum names change
- **Impact**: Database bloat and potential breaking changes
- **Fix**: Use integer storage with proper indexing: `entity.Property(e => e.Status).HasConversion<int>()`

### LOW: Missing cancellation token propagation
- **File**: src/Payouts/Application/Disbursements/Services/DisbursementService.cs:154,167
- **Issue**: `SaveChangesAsync(CancellationToken.None)` ignores cancellation tokens
- **Impact**: Operations can't be cancelled during shutdown, causing delays
- **Fix**: Pass through cancellation token: `await _context.SaveChangesAsync(innerCt);`

### LOW: Inconsistent error handling in gateway calls
- **File**: src/Payouts/Infrastructure/Gateways/StripePayoutGateway.cs
- **Issue**: No specific handling of different Stripe error types (rate limits, invalid account, insufficient balance)
- **Impact**: All errors treated the same, making debugging difficult
- **Fix**: Catch specific Stripe exceptions and map to domain-specific error types

## Summary

- **Total findings**: 17 (3 Critical, 4 High, 6 Medium, 4 Low)
- **Top 3 most urgent items**:
  1. Fix non-idempotent payout retries (financial impact)
  2. Resolve deadlock risk in parallel processing (system stability)
  3. Correct commission calculation inconsistency (business logic correctness)
- **Overall service health rating**: 6/10 - Core financial logic is sound but has several critical operational risks that need immediate attention before production deployment

The service demonstrates good architectural patterns (three-phase handlers, double-entry bookkeeping) but requires fixes for production reliability and financial accuracy.
