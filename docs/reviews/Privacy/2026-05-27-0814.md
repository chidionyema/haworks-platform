=== Continuous Review: Privacy (2026-05-27T08:14:38Z) ===
Files to review: 26

## CRITICAL: Developer Exception Page Enabled in Production
- **File**: src/Privacy/Privacy.Api/Program.cs:42
- **Issue**: `app.UseDeveloperExceptionPage();` is called unconditionally, exposing detailed error information in production
- **Impact**: Sensitive internal information (stack traces, connection strings, file paths) exposed to attackers
- **Fix**: Move inside development environment check:
```csharp
if (app.Environment.IsDevelopment())
{
    app.UseDeveloperExceptionPage();
    app.UseSwagger();
    app.UseSwaggerUI();
}
```

## CRITICAL: Service-to-Service Authorization Bypass
- **File**: src/Privacy/Privacy.Api/Controllers/PrivacyRequestsController.cs:28
- **Issue**: TODO comment explicitly states "IDOR risk" - privacy:erase scope is only logged, not enforced
- **Impact**: Compromised service tokens can perform privacy erasure on any user without proper authorization scope
- **Fix**: Enforce the scope check:
```csharp
if (!hasEraseScope)
{
    _logger.LogWarning("Privacy erasure denied for service {Service} - missing privacy:erase scope", initiatingService);
    return Forbid("privacy:erase scope required");
}
```

## CRITICAL: Race Condition in Pessimistic Locking
- **File**: src/Privacy/Privacy.Application/Requests/Commands/InitiateRequest/InitiatePrivacyRequestCommand.cs:42-47
- **Issue**: `FOR UPDATE SKIP LOCKED` followed by unprotected re-query creates race condition window
- **Impact**: Multiple concurrent requests for same user can both pass the "no active request" check and create duplicate records
- **Fix**: Use single atomic query with proper error handling:
```csharp
// Single query with proper locking - no SKIP LOCKED
await _context.Database.ExecuteSqlInterpolatedAsync(
    $"SELECT 1 FROM privacy.\"PrivacyRequests\" WHERE \"UserId\" = {userId} AND (\"Status\" = {pending} OR \"Status\" = {inProgress}) FOR UPDATE",
    cancellationToken);
```

## CRITICAL: Unique Constraint Exception Race Condition
- **File**: src/Privacy/Privacy.Application/Requests/Commands/InitiateRequest/InitiatePrivacyRequestCommand.cs:71-80
- **Issue**: After catching unique constraint violation, re-query without any locking protection
- **Impact**: Race condition where concurrent transaction can delete the "existing" record between exception and re-query
- **Fix**: Use the pessimistic lock approach consistently, don't rely on exception handling for concurrency control

## CRITICAL: Missing Database Constraint
- **File**: Database schema lacks unique constraint on (UserId, Status) for active requests
- **Issue**: Application assumes unique active request per user but no DB constraint enforces this
- **Impact**: Multiple active privacy requests per user possible, violating business rules
- **Fix**: Add migration for unique partial index:
```sql
CREATE UNIQUE INDEX IX_PrivacyRequests_UserId_ActiveStatus 
ON privacy."PrivacyRequests" ("UserId") 
WHERE "Status" IN (0, 1); -- Pending, InProgress
```

### HIGH: Domain Entity Concurrency Violations  
- **File**: src/Privacy/Privacy.Domain/Aggregates/PrivacyRequest.cs:18-32
- **Issue**: `Complete()` and `Fail()` methods have check-then-act race conditions without concurrency protection
- **Impact**: Two threads can simultaneously check status and both proceed to modify state
- **Fix**: Add version field and optimistic concurrency:
```csharp
public DateTimeOffset LastModified { get; set; }
// Use [ConcurrencyCheck] attribute or IsConcurrencyToken() in EF configuration
```

### HIGH: Saga State Machine Logic Flaw
- **File**: src/Privacy/Privacy.Application/Requests/Sagas/PrivacyRequestStateMachine.cs:168-180
- **Issue**: No protection against service reporting both completion AND failure for same request
- **Impact**: Saga state can become inconsistent if service sends contradictory events
- **Fix**: Add state validation in RecordCompletion and RecordFailure methods:
```csharp
private void RecordCompletion(PrivacyRequestState saga, string serviceName)
{
    if (saga.FailedServicesSet.Contains(serviceName))
    {
        _logger.LogWarning("Service {Service} already failed, ignoring completion", serviceName);
        return;
    }
    // ... rest of method
}
```

### HIGH: Data Consistency Gap
- **File**: Saga state (PrivacyRequestState) and domain entities (PrivacyRequest) track similar information independently
- **Issue**: No mechanism ensures consistency between saga state and domain entities
- **Impact**: Status shown in API may not match actual saga progression state
- **Fix**: Remove redundant PrivacyRequest status tracking, query saga state for status, or implement event sourcing pattern

### HIGH: Missing State Transition Validation
- **File**: src/Privacy/Privacy.Domain/Aggregates/PrivacyRequest.cs:18-32  
- **Issue**: Domain allows arbitrary state transitions without validation
- **Impact**: Invalid state transitions possible (e.g., Completed → Pending)
- **Fix**: Add state machine validation:
```csharp
public void Complete(Guid? contentId = null)
{
    if (Status is not (PrivacyRequestStatus.Pending or PrivacyRequestStatus.InProgress))
        throw new InvalidOperationException($"Cannot complete request in {Status} state");
    // ... rest
}
```

### MEDIUM: Background Worker Exception Handling
- **File**: src/Privacy/Privacy.Infrastructure/Workers/ErasureStalledWatcher.cs:59-67
- **Issue**: Workers catch all exceptions and continue, potentially masking systematic issues
- **Impact**: Critical failures might be ignored, leading to degraded service without alerting
- **Fix**: Distinguish between transient and fatal errors:
```csharp
catch (OperationCanceledException) when (stoppingToken.IsCancellationRequested) { }
catch (DbException ex)
{
    _logger.LogCritical(ex, "Database failure in watcher - stopping service");
    throw; // Let host handle restart
}
catch (Exception ex)
{
    _logger.LogError(ex, "Transient watcher failure");
}
```

### MEDIUM: Inefficient Database Access Pattern  
- **File**: src/Privacy/Privacy.Application/Requests/Commands/InitiateRequest/InitiatePrivacyRequestCommand.cs:42-53
- **Issue**: Two separate database round-trips (lock query + read query)
- **Impact**: Increased latency, potential timeout issues under load
- **Fix**: Combine into single query returning the locked record or null

### MEDIUM: Confusing DateTime Fallback
- **File**: src/Privacy/Privacy.Application/Requests/Queries/GetErasureStatus/GetErasureStatusQuery.cs:45
- **Issue**: Uses `DateTime.MinValue` as fallback for CreatedAt
- **Impact**: Could cause confusion in monitoring (shows year 0001)
- **Fix**: Use nullable DateTime and handle gracefully:
```csharp
CreatedAt: saga.CreatedAt ?? DateTime.UtcNow, // or throw exception
```

### LOW: Complex Controller Authorization Logic
- **File**: src/Privacy/Privacy.Api/Controllers/PrivacyRequestsController.cs:24-48
- **Issue**: Complex service vs user authorization logic mixed in controller  
- **Impact**: Hard to unit test, maintain, and audit
- **Fix**: Extract to authorization service or policy-based authorization

### LOW: Security Monitoring Gap
- **File**: src/Privacy/Privacy.Application/Requests/Queries/GetErasureStatus/GetErasureStatusQuery.cs:44-50
- **Issue**: IDOR attempts only logged as warnings, not tracked as security events
- **Impact**: Harder to detect attack patterns or compromised accounts
- **Fix**: Use structured logging with security event classification

## Summary
- **CRITICAL**: 5 findings - All require immediate attention before production
- **HIGH**: 5 findings - Address in current sprint
- **MEDIUM**: 4 findings - Plan for next release  
- **LOW**: 2 findings - Technical debt backlog

**Top 3 Most Urgent:**
1. Fix production developer exception page exposure (immediate security risk)
2. Enforce privacy:erase scope validation (prevents unauthorized data access)
3. Resolve race conditions in privacy request creation (data integrity)

**Overall Service Health Rating: 4/10**

This service has significant security vulnerabilities and race conditions that make it unsuitable for production without immediate fixes. The core business logic is sound and well-tested, but critical infrastructure concerns need resolution.
