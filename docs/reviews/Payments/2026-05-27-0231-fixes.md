## Fixes Applied
- [StripeKeyRotationController.cs:39] Fixed broken idempotency pattern by deriving key from NewSecretKey using SHA256 hash instead of random GUID
- [RefundsController.cs:25] Fixed broken idempotency pattern by accepting optional client IdempotencyKey with deterministic fallback from PaymentId+AmountCents+Currency 
- [SubscriptionsController.cs:44,60,76] Fixed broken idempotency patterns across all three endpoints (create, cancel, resume) with optional client keys and deterministic fallbacks
- [WebhooksController.cs:202] Fixed GUID collision risk by implementing proper UUID v5 generation with version/variant bits instead of truncated SHA256
- [PaymentSessionRequestedConsumer.cs:148] Fixed GUID collision risk using same UUID v5 approach with proper version/variant bits
- [PaymentSessionRequestedConsumer.cs:187] Made hardcoded 1000ms demo delay configurable via `Payments:DemoDelayMs` configuration setting

## Skipped (too risky or ambiguous)
- [Missing Payment Ownership Validation in RefundsController] Requires adding payment repository dependency and changing controller interface, potentially breaking other services
- [Add pagination to PaymentRepository.ListByUserAsync] Requires changing IPaymentRepository interface which could break other services  
- [Add domain events to Payment.ReverseRefund method] Requires architectural changes to add domain event infrastructure and new contracts, too complex for surgical fix

All fixes successfully compiled with no errors (105 warnings, 0 errors). The three most critical security and reliability issues have been resolved: broken idempotency patterns that could cause duplicate payments/refunds, and GUID collision risks that could break message deduplication.
