=== Continuous Review: CheckoutOrchestrator (2026-05-27T17:15:34Z) ===
Files to review: 34

Looking at the CheckoutOrchestrator service, I'll perform a comprehensive code review across all specified areas.

## CRITICAL: Invalid Amount Handling in Saga

### CRITICAL: Saga continues with invalid amounts
- **File**: src/CheckoutOrchestrator/CheckoutOrchestrator.Application/Sagas/CheckoutSaga.cs:84
- **Issue**: When `TotalAmountCents <= 0`, the saga sets amount to 0 and logs a warning but continues to `Initiated` state and publishes `StockReservationRequestedEvent`
- **Impact**: Invalid transactions will proceed through the checkout flow, potentially causing downstream failures and data corruption
- **Fix**: 
```csharp
if (msg.TotalAmountCents <= 0)
{
    sagaState.TotalAmountCents = 0;
    sagaState.FailureReason = "invalid_amount";
    logger.LogWarning(...);
    // CRITICAL FIX: Don't continue the flow
    return;
}
// Add immediate transition to Abandoned state instead
.TransitionTo(Abandoned);
```

## CRITICAL: Data Integrity Issues

### CRITICAL: Currency migration precision loss
- **File**: src/CheckoutOrchestrator/CheckoutOrchestrator.Infrastructure/Migrations/20260526120003_TotalAmountCents.cs:16
- **Issue**: Migration uses SQL `ROUND()` which differs from .NET's `MidpointRounding.AwayFromZero`
- **Impact**: Financial data corruption during migration - different rounding could cause penny differences
- **Fix**: Use a more precise conversion or validate post-migration that amounts match expected rounding

### CRITICAL: Idempotency broken by fallback logic
- **File**: src/CheckoutOrchestrator/CheckoutOrchestrator.Application/Commands/StartCheckoutCommand.cs:28
- **Issue**: Falls back to `Guid.NewGuid()` when `IdempotencyKey` is null/empty
- **Impact**: Same request without idempotency key creates multiple saga instances
- **Fix**: Require idempotency key or derive from OrderId consistently
```csharp
var hashInput = !string.IsNullOrEmpty(request.IdempotencyKey)
    ? request.IdempotencyKey
    : request.OrderId.ToString("N"); // Use OrderId for consistency
```

## HIGH: Security Vulnerabilities

### HIGH: Missing currency validation
- **File**: src/CheckoutOrchestrator/CheckoutOrchestrator.Api/Models/StartCheckoutRequest.cs:22
- **Issue**: Currency accepts any string without validation
- **Impact**: Injection attacks, invalid currencies propagated through system
- **Fix**: Add validation attribute with currency whitelist
```csharp
[RegularExpression("^(USD|EUR|GBP|CAD)$", ErrorMessage = "Invalid currency")]
public string Currency { get; init; } = "USD";
```

### HIGH: Potential integer overflow
- **File**: src/CheckoutOrchestrator/CheckoutOrchestrator.Api/Controllers/CheckoutsController.cs:25
- **Issue**: Unchecked conversion from decimal to long
- **Impact**: Arithmetic overflow could cause negative amounts or exceptions
- **Fix**: Add bounds checking before conversion
```csharp
if (body.TotalAmount > (decimal.MaxValue / 100m))
    return BadRequest("Amount too large");
```

## HIGH: Authorization Logic Gaps

### HIGH: Insufficient user validation
- **File**: src/CheckoutOrchestrator/CheckoutOrchestrator.Application/QueryHandlers/GetCheckoutSagaQueryHandler.cs:17
- **Issue**: Trusts `request.UserId` without validating it matches authenticated user
- **Impact**: IDOR vulnerability - users could access other users' checkout data by manipulating UserId
- **Fix**: Get UserId from authenticated context, not request parameter

## MEDIUM: Logic & Edge Case Issues

### MEDIUM: Inconsistent nullability
- **File**: src/CheckoutOrchestrator/CheckoutOrchestrator.Api/Models/StartCheckoutRequest.cs:22 vs CheckoutSaga.cs:96
- **Issue**: Currency is nullable in request but required in saga
- **Impact**: Potential null reference exceptions in saga
- **Fix**: Make Currency required in request DTO or handle null properly in saga

### MEDIUM: Duplicate validator files
- **File**: Multiple files with identical validators
- **Issue**: `CheckoutSagaQueryValidators.cs` and `GetCheckoutSagaQueryValidator.cs` contain duplicate code
- **Impact**: Maintenance burden, potential for divergence
- **Fix**: Consolidate into single file

### MEDIUM: Unbounded background worker queries
- **File**: src/CheckoutOrchestrator/CheckoutOrchestrator.Infrastructure/Workers/PaymentExpiryWatcher.cs:84
- **Issue**: Queries all matching sagas before applying TAKE limit
- **Impact**: Performance degradation with large datasets
- **Fix**: Add database-level pagination with cursor-based iteration

## MEDIUM: Missing Database Constraints

### MEDIUM: No amount validation at DB level
- **File**: src/CheckoutOrchestrator/CheckoutOrchestrator.Infrastructure/CheckoutDbContext.cs:53
- **Issue**: No database constraint ensuring `TotalAmountCents > 0`
- **Impact**: Invalid data could persist despite application-level validation
- **Fix**: Add check constraint
```csharp
entity.Property(s => s.TotalAmountCents)
    .HasColumnType("bigint")
    .HasCheckConstraint("CK_CheckoutSagas_TotalAmountCents_Positive", "\"TotalAmountCents\" > 0");
```

## LOW: Code Quality Issues

### LOW: Missing cancellation token propagation
- **File**: Multiple background workers
- **Issue**: Some async operations don't propagate cancellation tokens consistently
- **Impact**: Graceful shutdown may be delayed
- **Fix**: Ensure all async calls accept cancellation tokens

### LOW: String comparison hardcoding
- **File**: src/CheckoutOrchestrator/CheckoutOrchestrator.Application/Sagas/CheckoutSaga.cs:294
- **Issue**: Manual string comparisons with state names could break if state names change
- **Impact**: Brittle code, potential runtime errors
- **Fix**: Use state object comparisons instead of string matching

### LOW: JSON deserialization exception handling
- **File**: src/CheckoutOrchestrator/CheckoutOrchestrator.Application/Sagas/CheckoutSaga.cs:365
- **Issue**: `DeserializeItems()` throws on invalid JSON but not all callers handle gracefully
- **Impact**: Saga could get stuck in error state
- **Fix**: Consider graceful degradation or retry logic

## Summary

- **CRITICAL**: 3 findings (invalid amount flow, migration precision, idempotency)
- **HIGH**: 3 findings (currency validation, integer overflow, authorization)
- **MEDIUM**: 4 findings (nullability, duplicates, performance, constraints) 
- **LOW**: 3 findings (cancellation, string comparisons, exception handling)

### Top 3 Most Urgent Items:
1. **Fix invalid amount handling in saga** - prevents financial corruption
2. **Validate currency migration data** - ensure no precision loss occurred
3. **Implement proper currency validation** - prevent injection attacks

### Overall Service Health Rating: 5/10

The service has solid architecture and good error recovery mechanisms, but contains critical financial logic bugs and security vulnerabilities that must be addressed before production use. The saga pattern implementation is sophisticated but needs refinement in edge case handling.
