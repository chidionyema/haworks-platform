=== Continuous Review: Catalog (2026-05-31T13:55:53Z) ===
Files to review: 90

I'll perform a comprehensive code review of the Catalog service, examining it through all 12 architectural lenses.

## CRITICAL: Information Disclosure in Stock Reservation Error Messages

### CRITICAL: Sensitive Stock Data Leaked in Error Messages
- **File**: src/Catalog/Catalog.Application/Commands/ReserveStockCommand.cs:97
- **Issue**: Error message exposes internal stock quantities: `"Insufficient stock for product {product.Id}: requested {request.Quantity}, available {product.StockQuantity}"`
- **Impact**: Attackers can probe stock levels by attempting reservations, revealing business-sensitive inventory data
- **Fix**: Remove stock quantity from error message or return generic "insufficient stock" without specific numbers

## HIGH SEVERITY ISSUES

### HIGH: Cache Invalidation Race Condition  
- **File**: src/Catalog/Catalog.Application/Commands/UpdateProductCommand.cs:81-84
- **Issue**: Cache invalidation occurs AFTER `SaveChangesAsync`, creating a race window where concurrent readers can fetch stale data and repopulate the cache before invalidation
- **Impact**: Stale product data could persist in cache until TTL expires, showing incorrect prices/availability
- **Fix**: Move `InvalidateAsync` call before `SaveChangesAsync`, or use a two-phase invalidation pattern

### HIGH: Unvalidated Currency Input
- **File**: src/Catalog/Catalog.Api/Controllers/ProductsController.cs:96, 111  
- **Issue**: `Money.FromMajorUnits(body.UnitPrice, body.Currency)` called without validating currency format first
- **Impact**: Invalid currency codes could cause exceptions, potential DoS via malformed requests
- **Fix**: Add currency validation in FluentValidation rules or validate before calling Money.FromMajorUnits

### HIGH: Integer Overflow in Stock Release
- **File**: src/Catalog/Catalog.Domain/Product.cs:107
- **Issue**: `StockQuantity += quantity` doesn't check for integer overflow
- **Impact**: Malicious or buggy release requests could cause integer wraparound, corrupting stock counts
- **Fix**: Add overflow check: `if (StockQuantity > int.MaxValue - quantity) throw new ArgumentException("Stock overflow");`

## MEDIUM SEVERITY ISSUES

### MEDIUM: Misleading Response on Category Creation Failure
- **File**: src/Catalog/Catalog.Api/Controllers/CategoriesController.cs:20
- **Issue**: `ToCreatedActionResult` returns `Guid.NewGuid()` when result fails, providing misleading response
- **Impact**: Clients receive fake IDs for failed operations, breaking error handling assumptions
- **Fix**: Return actual error response instead of generated ID: `return result.ToActionResult();`

### MEDIUM: Potential DbContext Lifecycle Issue  
- **File**: src/Catalog/Catalog.Infrastructure/Services/StockService.cs:45-90
- **Issue**: Complex transaction with multiple database operations in single method could lead to connection exhaustion under load
- **Impact**: Connection pool starvation during high reservation volume
- **Fix**: Consider breaking into smaller, focused methods or using connection pooling optimizations

### MEDIUM: Missing Bounds Check in Cache Stampede Demo
- **File**: src/Catalog/Catalog.Api/Controllers/DemoTestController.cs:139
- **Issue**: `Math.Clamp(request.ConcurrentRequests, ?, ?)` - no upper bound specified for concurrent requests
- **Impact**: Demo endpoint could be abused to create excessive concurrent load
- **Fix**: Add reasonable upper bound: `Math.Clamp(request.ConcurrentRequests, 1, 100)`

### MEDIUM: Incomplete Error Context in Stock Release Consumer
- **File**: src/Catalog/Catalog.Application/Consumers/StockReleaseRequestedConsumer.cs:86
- **Issue**: When product not found during stock release, logs warning but continues processing other items without comprehensive error context
- **Impact**: Silent failures could leave partial releases, making debugging difficult
- **Fix**: Collect and return comprehensive error information for all failed items

## LOW SEVERITY ISSUES

### LOW: Redundant Null Check
- **File**: src/Catalog/Catalog.Api/Controllers/ProductReviewsController.cs:60
- **Issue**: `string.IsNullOrEmpty(userId)` check after `HttpContext.GetForwardedUserId()` which already handles nulls via extension method
- **Impact**: None, but indicates defensive programming where not needed
- **Fix**: Remove redundant check or add comment explaining why it's needed

### LOW: Magic Number in Reservation TTL
- **File**: src/Catalog/Catalog.Application/Commands/Reservations/CreateReservationCommandHandler.cs:32
- **Issue**: Hard-coded `TimeSpan.FromMinutes(15)` without configuration
- **Impact**: Inflexible reservation timeout, requires code change to adjust business rules  
- **Fix**: Move to configuration: `IOptions<ReservationOptions>`

### LOW: Inconsistent Async Patterns
- **File**: src/Catalog/Catalog.Infrastructure/Repositories/ProductReviewRepository.cs:29
- **Issue**: `UpdateAsync` method is sync but returns `Task.CompletedTask`
- **Impact**: Misleading method signature, could confuse maintenance developers
- **Fix**: Either make truly async or rename to `Update` (sync)

## DATABASE & MIGRATION INTEGRITY

### MEDIUM: Missing Index on Hot Query Path
- **File**: Database schema analysis
- **Issue**: No index on `StockReservations(UserId)` but sweeper queries by user frequently
- **Impact**: Poor query performance as reservation table grows
- **Fix**: Add index in migration: `entity.HasIndex(r => r.UserId)`

### LOW: Potential Schema Drift  
- **File**: Multiple entity configurations in CatalogDbContext.cs
- **Issue**: Some properties use `.HasMaxLength()` while others rely on defaults, inconsistent schema definition
- **Impact**: Could lead to database/model mismatches during deployment
- **Fix**: Explicitly specify all string lengths and constraints

## SECURITY REVIEW

### CRITICAL: See "Information Disclosure" above

### MEDIUM: IDOR Prevention Incomplete  
- **File**: src/Catalog/Catalog.Api/Controllers/ReservationsController.cs:94
- **Issue**: Reservation confirmation checks user ownership but uses string comparison without normalization
- **Impact**: Case-sensitivity issues could allow unauthorized access in some auth configurations  
- **Fix**: Use `StringComparison.OrdinalIgnoreCase` or ensure auth system normalizes user IDs

## PERFORMANCE & SCALABILITY

### MEDIUM: Potential N+1 in Product Listing
- **File**: src/Catalog/Catalog.Infrastructure/Repositories/ProductRepository.cs:17-18
- **Issue**: `Include(p => p.Category)` loads full category for each product, but List endpoint projects `categoryName` as null
- **Impact**: Unnecessary data fetching reduces throughput
- **Fix**: Remove Include or add projection to only fetch needed fields

### LOW: Unbounded Query in Category List
- **File**: src/Catalog/Catalog.Infrastructure/Repositories/CategoryRepository.cs:11
- **Issue**: `ListAsync` has no Take() limit, could return thousands of categories
- **Impact**: Memory exhaustion with large category trees
- **Fix**: Add pagination parameters and default limits

## Summary

- **CRITICAL**: 1 finding (information disclosure)
- **HIGH**: 3 findings (cache race, currency validation, integer overflow)  
- **MEDIUM**: 6 findings (misleading responses, performance issues, security gaps)
- **LOW**: 4 findings (code quality, maintainability issues)

**Top 3 Most Urgent Items:**
1. **CRITICAL**: Fix information disclosure in stock reservation error messages - immediate security risk
2. **HIGH**: Add currency validation before Money.FromMajorUnits calls - prevents application crashes
3. **HIGH**: Fix cache invalidation race condition - ensures data consistency

**Overall Service Health Rating: 7/10**

The Catalog service demonstrates solid architectural patterns with proper domain modeling, event sourcing, and transactional outbox implementation. However, several security and data integrity issues need addressing before production deployment. The codebase shows good understanding of concurrency patterns but has some gaps in defensive programming and input validation.
