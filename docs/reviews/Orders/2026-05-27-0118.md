=== Continuous Review: Orders (2026-05-27T01:18:55Z) ===
Files to review: 45

## CRITICAL: Potential Integer Overflow in Financial Calculations
- **File**: src/Orders/Orders.Domain/OrderItem.cs:47
- **Issue**: `public long LineTotalCents => Quantity * UnitPriceCents;` can overflow when both values are large
- **Impact**: Could cause negative totals or wrong calculations in production, leading to financial loss
- **Fix**: Add overflow checking: `public long LineTotalCents => checked(Quantity * UnitPriceCents);`

### CRITICAL: Deterministic GUID Collision Risk
- **File**: src/Orders/Orders.Application/Commands/CreateOrderCommand.cs:43-44
- **Issue**: `new Guid(SHA256.HashData(Encoding.UTF8.GetBytes(request.UserId)).AsSpan(0, 16))` only uses first 16 bytes of SHA256 hash
- **Impact**: Higher collision probability than expected, could cause saga correlation failures
- **Fix**: Use a proper deterministic UUID algorithm or cryptographically secure method

### HIGH: Race Condition in Demo Controller
- **File**: src/Orders/Orders.Api/Controllers/DemoIdempotencyController.cs:122-124
- **Issue**: Static field `s_tableInitialized` uses Volatile but table creation isn't synchronized
- **Impact**: Concurrent requests could cause duplicate table creation attempts or inconsistent state
- **Fix**: Use proper locking mechanism or make table creation idempotent at the application level

### HIGH: Case-Insensitive Email Comparison Security Issue
- **File**: src/Orders/Orders.Application/Queries/GetGuestOrderQuery.cs:31
- **Issue**: `string.Equals(guestInfo.Email, request.Email, StringComparison.OrdinalIgnoreCase)`
- **Impact**: Account enumeration attack vector - attacker can probe for email addresses
- **Fix**: Use case-sensitive comparison: `StringComparison.Ordinal`

### HIGH: Misleading Error Handling in Race Demo
- **File**: src/Orders/Orders.Api/Controllers/DemoIdempotencyController.cs:184-193
- **Issue**: On exception, returns fake success data with `isWinner = false` and random GUID
- **Impact**: Masks real failures and provides misleading data to clients
- **Fix**: Return proper error response or null result instead of fake data

### MEDIUM: Missing Maximum Value Validation
- **File**: src/Orders/Orders.Application/Validators/CreateOrderCommandValidator.cs:11-12
- **Issue**: No upper bounds on `TotalAmountCents` and `Quantity` values
- **Impact**: Could allow unreasonably large values leading to overflow or DoS
- **Fix**: Add reasonable maximum limits: `.LessThan(long.MaxValue / 1000)` for amounts, `.LessThan(10000)` for quantities

### MEDIUM: Memory Accumulation in Privacy Erasure
- **File**: src/Orders/Orders.Application/Consumers/PrivacyErasureRequestedConsumer.cs:35-47
- **Issue**: Loads batches but doesn't call `SaveChangesAsync()` until end, accumulating changes in memory
- **Impact**: High memory usage for users with many orders, potential OOM
- **Fix**: Call `SaveChangesAsync()` after each batch

### MEDIUM: Information Leakage in Error Messages
- **File**: src/Orders/Orders.Api/Controllers/DemoIdempotencyController.cs:109
- **Issue**: Returns `ex.Message` in 503 response, potentially exposing internal details
- **Impact**: Could leak sensitive infrastructure information to attackers
- **Fix**: Return generic error message, log details server-side only

### MEDIUM: Inconsistent Pagination Validation
- **File**: src/Orders/Orders.Api/Controllers/OrdersController.cs:29
- **Issue**: Clamps `take` to 100 but `ListUserOrdersQueryValidator` allows different range
- **Impact**: Inconsistent behavior between validation and runtime
- **Fix**: Align validation rules with runtime clamping logic

### MEDIUM: Missing Cancellation Token Propagation
- **File**: src/Orders/Orders.Domain/OrderItem.cs:49-60
- **Issue**: `Create` method and other domain methods don't accept `CancellationToken`
- **Impact**: Cannot cancel long-running operations during high load
- **Fix**: Add cancellation token parameters where appropriate

### LOW: Hardcoded Magic Strings
- **File**: src/Orders/Orders.Domain/Order.cs:150
- **Issue**: `UserId = "ERASED"` and `CustomerEmail = "deleted@privacy.invalid"` are hardcoded
- **Impact**: Difficult to maintain or change privacy compliance requirements
- **Fix**: Move to configuration or constants

### LOW: Missing Input Sanitization
- **File**: src/Orders/Orders.Api/Controllers/DemoIdempotencyController.cs:153
- **Issue**: No validation that `body.Key` doesn't contain SQL injection characters (though parameterized queries protect)
- **Impact**: Potential for log injection or other side effects
- **Fix**: Add input sanitization for demo endpoints

### LOW: Inconsistent Error Response Format
- **File**: src/Orders/Orders.Api/Controllers/DemoIdempotencyController.cs:32-34
- **Issue**: Returns anonymous objects instead of consistent error DTOs
- **Impact**: API clients cannot rely on consistent error handling
- **Fix**: Use standard error response DTOs throughout

### LOW: Missing Business Rule Validation
- **File**: src/Orders/Orders.Domain/Order.cs:130-140
- **Issue**: `MarkAbandoned` and `MarkExpired` both set `AbandonReason` but don't validate reason format
- **Impact**: Inconsistent audit trail data quality
- **Fix**: Add validation for reason format and length

### LOW: Potential N+1 Query Pattern
- **File**: src/Orders/Orders.Infrastructure/Repositories/OrderRepository.cs:15-17
- **Issue**: While using `Include(o => o.Items)`, the StatusHistory collection isn't included but may be accessed
- **Impact**: Could cause N+1 queries if StatusHistory is accessed later
- **Fix**: Add explicit include for StatusHistory or document the limitation

## Summary
- **CRITICAL**: 2 findings (integer overflow, GUID collision)
- **HIGH**: 3 findings (race condition, security issue, error handling)
- **MEDIUM**: 6 findings (validation, memory usage, information leakage)
- **LOW**: 6 findings (maintainability and consistency issues)

**Top 3 Most Urgent Items:**
1. Fix integer overflow in LineTotalCents calculation (financial impact)
2. Address deterministic GUID collision risk (saga correlation failures)
3. Fix case-insensitive email comparison (security vulnerability)

**Overall Service Health Rating: 6/10**

The service has solid architecture and follows many best practices (good use of domain patterns, proper transaction boundaries, MassTransit outbox), but has several critical financial and security vulnerabilities that need immediate attention. The concurrency handling could be improved, and there are various edge cases that aren't properly handled.
