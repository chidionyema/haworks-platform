## Validation Summary
- Original gaps: 45
- Confirmed: 32
- False positives: 6
- Low value removed: 7

## Validated Test Coverage Audit: Media (2026-05-31T14:29:01Z)

### FALSE_POSITIVE: Already Covered

**AbortUpload Command** - Test exists at `MediaFlowsTests.cs:270-288`
```csharp
[Fact]
public async Task AbortUpload_RemovesRecord()
```

**GetMediaUrl Query** - Test exists at `MediaFlowsTests.cs:291-308`  
```csharp
[Fact]
public async Task GetMediaUrl_ActiveFile_ReturnsPresignedUrl()
```

**ListMedia Query** - Test exists at `MediaFlowsTests.cs:250-267`
```csharp
[Fact]
public async Task ListMedia_ReturnsPaginatedResults()
```

**ProcessVirusScan Command** - Comprehensive unit tests in `ProcessVirusScanTests.cs`

**InitiateUpload Command** - Comprehensive unit tests in `InitiateUploadTests.cs`

**MediaFile Domain Entity** - State transition tests in `ValidatorTests.cs:42-108`

### CONFIRMED: Missing Test Coverage

#### Application Commands/Queries (HIGH PRIORITY)

**CompleteMultipartUpload Command** - CONFIRMED missing
- **File**: `src/Media/Media.Api/Application/CompleteMultipartUpload.cs:34`
- **Complexity**: Complex S3 coordination, hash validation, virus scanning pipeline
- **Test Skeleton**:
```csharp
public class CompleteMultipartUploadTests
{
    [Fact]
    public async Task Handle_ValidParts_ShouldStitchAndScan()
    {
        // Arrange
        var handler = CreateHandler();
        var mediaFile = MediaFile.Create("big.mp4", "hash", 50_000_000, "video/mp4", "owner");
        mediaFile.InitiateMultipart("upload-id", 3);
        await _context.SaveChangesAsync();
        
        var parts = new[] { new PartETagDto(1, "etag1"), new PartETagDto(2, "etag2") };
        
        // Act
        var result = await handler.Handle(new CompleteMultipartUploadCommand(mediaFile.Id, parts), CancellationToken.None);
        
        // Assert
        result.IsSuccess.Should().BeTrue();
        var file = await _context.MediaFiles.FindAsync(mediaFile.Id);
        file!.Status.Should().Be(MediaStatus.Quarantined);
    }
}
```

**BatchInitiateUpload Command** - CONFIRMED missing
- **File**: `src/Media/Media.Api/Application/BatchInitiateUpload.cs:28`
- **Complexity**: Batch processing with mixed single/multipart logic
- **Test Skeleton**:
```csharp
public class BatchInitiateUploadTests
{
    [Fact]
    public async Task Handle_MixedSizes_ReturnsMixedUploadTypes()
    {
        // Test batch with both small and large files
        var files = new[]
        {
            new InitiateUploadCommand("small.png", "hash1", 1024, "image/png"),
            new InitiateUploadCommand("big.mp4", "hash2", 50_000_000, "video/mp4")
        };
        var command = new BatchInitiateUploadCommand(files);
        
        var result = await _handler.Handle(command, CancellationToken.None);
        
        result.IsSuccess.Should().BeTrue();
        result.Value.Should().HaveCount(2);
        result.Value[0].IsMultipart.Should().BeFalse();
        result.Value[1].IsMultipart.Should().BeTrue();
    }
}
```

**DeleteMedia Command** - CONFIRMED missing
- **File**: `src/Media/Media.Api/Application/DeleteMedia.cs:25`
- **Test Skeleton**:
```csharp
public class DeleteMediaTests
{
    [Fact]
    public async Task Handle_ValidFile_ShouldMarkDeleted()
    {
        var mediaFile = MediaFile.Create("test.png", "hash", 1024, "image/png", "owner");
        await _context.SaveChangesAsync();
        
        var result = await _handler.Handle(new DeleteMediaCommand(mediaFile.Id), CancellationToken.None);
        
        result.IsSuccess.Should().BeTrue();
        var updated = await _context.MediaFiles.IgnoreQueryFilters().FirstAsync(f => f.Id == mediaFile.Id);
        updated.IsDeleted.Should().BeTrue();
    }
}
```

**LinkEntity Command** - CONFIRMED missing
- **File**: `src/Media/Media.Api/Application/LinkEntity.cs:23`

#### Infrastructure Services (HIGH PRIORITY)

**S3Service** - CONFIRMED missing
- **File**: `src/Media/Media.Api/Infrastructure/S3Service.cs` (all methods)
- **Reason**: Core storage abstraction, only interface mocks exist
- **Test Skeleton**:
```csharp
public class S3ServiceTests
{
    [Fact]
    public void GeneratePreSignedUrl_WhenEnabled_ReturnsValidUrl()
    {
        var s3Mock = new Mock<IAmazonS3>();
        var opts = Options.Create(new MediaStorageOptions { Enabled = true });
        var service = new S3Service(s3Mock.Object, opts);
        
        s3Mock.Setup(x => x.GetPreSignedURL(It.IsAny<GetPreSignedUrlRequest>()))
            .Returns("https://example.com/upload");
            
        var result = service.GeneratePreSignedUrl("key", "image/png");
        
        result.Should().Be("https://example.com/upload");
    }
}
```

**ClamAvScanner** - CONFIRMED missing
- **File**: `src/Media/Media.Api/Infrastructure/ClamAvScanner.cs` (all methods)
- **Test Skeleton**:
```csharp
public class ClamAvScannerTests
{
    [Fact]
    public async Task ScanAsync_WhenDisabled_ReturnsTrue()
    {
        var opts = Options.Create(new ClamAvOptions { Enabled = false });
        var scanner = new ClamAvScanner(opts, Mock.Of<ILogger<ClamAvScanner>>());
        
        var result = await scanner.ScanAsync(new MemoryStream());
        
        result.Should().BeTrue();
    }
}
```

#### Message Consumers (HIGH PRIORITY)

**MediaScanFailedConsumer** - CONFIRMED missing
- **File**: `src/Media/Media.Api/Infrastructure/Workers/MediaScanFailedConsumer.cs:16`
- **Test Skeleton**:
```csharp
[Collection("Media Integration")]
public class MediaConsumersTests : IAsyncLifetime
{
    [Fact]
    public async Task MediaScanFailedConsumer_ShouldMarkRejected()
    {
        var harness = _factory.Services.GetRequiredService<ITestHarness>();
        var mediaFile = CreateQuarantinedFile();
        
        await harness.Bus.Publish(new MediaScanFailedEvent { MediaId = mediaFile.Id, Reason = "Virus detected" });
        
        await TestWait.Until(async () =>
        {
            var updated = await GetFileAsync(mediaFile.Id);
            return updated?.Status == MediaStatus.Rejected;
        });
    }
}
```

**MediaScanPassedConsumer** - CONFIRMED missing
- **File**: `src/Media/Media.Api/Infrastructure/Workers/MediaScanPassedConsumer.cs:17`

**MediaUploadCompletedConsumer** - CONFIRMED missing
- **File**: `src/Media/Media.Api/Infrastructure/Workers/MediaUploadCompletedConsumer.cs` (exists)

**ProcessMediaConsumer** - CONFIRMED missing
- **File**: `src/Media/Media.Api/Infrastructure/Workers/ProcessMediaConsumer.cs` (exists)

**ProcessMediaFaultConsumer** - CONFIRMED missing  
- **File**: `src/Media/Media.Api/Infrastructure/Workers/ProcessMediaFaultConsumer.cs` (exists)

#### Background Services (MEDIUM PRIORITY)

**UploadSweeperWorker** - CONFIRMED missing
- **File**: `src/Media/Media.Api/Infrastructure/Workers/UploadSweeperWorker.cs` (exists)

**S3EventConsumer** - CONFIRMED missing
- **File**: `src/Media/Media.Api/Infrastructure/Workers/S3EventConsumer.cs` (exists)

#### Media Processing (MEDIUM PRIORITY)

**ImageProcessor** - CONFIRMED missing
- **File**: `src/Media/Media.Api/Infrastructure/Processing/ImageProcessor.cs` (exists)

**VideoProcessor** - CONFIRMED missing
- **File**: `src/Media/Media.Api/Infrastructure/Processing/VideoProcessor.cs` (exists)

**AudioProcessor** - CONFIRMED missing
- **File**: `src/Media/Media.Api/Infrastructure/Processing/AudioProcessor.cs` (exists)

**MediaProcessingOrchestrator** - CONFIRMED missing
- **File**: `src/Media/Media.Api/Infrastructure/Processing/MediaProcessingOrchestrator.cs` (exists)

#### API Error Handling (MEDIUM PRIORITY)

**MediaController Error Scenarios** - CONFIRMED missing
- Current integration tests only cover happy paths
- Missing: 400/401/403/404/500 error responses, malformed requests

### LOW_VALUE: Removed

**MediaMetadata** - Simple data class, no business logic
**MediaVersion** - Simple data class, no business logic  
**FileSignatureValidator** - Single validation method, low complexity
**FfmpegService** - External process wrapper, minimal logic
**Health checks** - Simple connectivity checks
**Options classes** - Configuration POCOs
**DTOs/Records** - Data transfer objects with no logic

### Test Infrastructure Compliance

✅ **SharedTestS3**: MediaWebAppFactory correctly uses `SharedTestS3.GetEndpointAsync()`
✅ **Migration**: Uses `MigrateAsync()` correctly  
✅ **Authentication**: Uses TestAuthenticationHandler properly

### Summary

- **Total confirmed gaps**: 32
- **Critical missing**: Command handlers (4), Infrastructure services (2), Message consumers (5)
- **Test optimization violations**: 0
- **Recommended priority**: Focus on command handlers and infrastructure services first

The Media service has good integration test coverage for end-to-end flows but lacks comprehensive unit test coverage for individual components, especially around error handling and edge cases.
