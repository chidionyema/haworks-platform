=== Test Coverage Audit: Media (2026-05-30T21:35:52Z) ===
Source files: 41
Unit test files: 3
Integration test files: 3
E2E test files: 14

Looking at the Media service source code and existing tests, I'll provide a comprehensive test coverage audit.

## Coverage Map

### Application Handlers
- [COVERED] InitiateUploadHandler — tested by InitiateUploadTests.Handle_*
- [COVERED] ProcessVirusScanHandler — tested by ProcessVirusScanTests.Handle_*
- [MISSING] AbortUploadHandler — no test found, PRIORITY: HIGH
- [MISSING] BatchInitiateUploadHandler — no test found, PRIORITY: MEDIUM
- [MISSING] CompleteMultipartUploadHandler — no test found, PRIORITY: HIGH
- [MISSING] DeleteMediaHandler — no test found, PRIORITY: HIGH
- [MISSING] GetMediaUrlHandler — no test found, PRIORITY: MEDIUM
- [MISSING] LinkEntityHandler — no test found, PRIORITY: LOW
- [MISSING] ListMediaHandler — no test found, PRIORITY: MEDIUM

### Domain Logic
- [PARTIAL] MediaFile — state transitions tested, missing: business rules validation, edge cases
- [MISSING] MediaMetadata — no test found, PRIORITY: LOW
- [MISSING] MediaVersion — no test found, PRIORITY: LOW

### Infrastructure Services
- [MISSING] S3Service — no test found, PRIORITY: MEDIUM
- [MISSING] ClamAvScanner — no test found, PRIORITY: HIGH
- [MISSING] FileSignatureValidator — no test found, PRIORITY: HIGH
- [MISSING] MediaProcessingOrchestrator — no test found, PRIORITY: MEDIUM
- [MISSING] ImageProcessor — no test found, PRIORITY: MEDIUM
- [MISSING] VideoProcessor — no test found, PRIORITY: MEDIUM
- [MISSING] AudioProcessor — no test found, PRIORITY: MEDIUM

### Background Workers
- [MISSING] ProcessMediaConsumer — no test found, PRIORITY: HIGH
- [MISSING] MediaUploadCompletedConsumer — no test found, PRIORITY: HIGH
- [MISSING] MediaScanPassedConsumer — no test found, PRIORITY: MEDIUM
- [MISSING] MediaScanFailedConsumer — no test found, PRIORITY: MEDIUM
- [MISSING] S3EventConsumer — no test found, PRIORITY: MEDIUM
- [MISSING] UploadSweeperWorker — no test found, PRIORITY: MEDIUM

### API Endpoints
- [COVERED] Most endpoints — tested by MediaFlowsTests
- [PARTIAL] Error scenarios — happy path only, missing: validation failures, auth failures

## Missing Tests (ordered by priority)

### HIGH PRIORITY: Core Business Logic

#### AbortUploadHandler Unit Tests
- **For**: AbortUpload.cs:AbortUploadHandler.Handle
- **Type**: Unit
- **Why**: Core upload lifecycle management - abort functionality must work correctly to prevent resource leaks
- **Skeleton**:
```csharp
[Fact]
public async Task Handle_PendingUpload_ShouldMarkDeletedAndAbortS3()
{
    // Arrange
    var mediaFile = MediaFile.Create("test.mp4", "hash", 50_000_000, "video/mp4", "owner");
    mediaFile.InitiateMultipart("upload-id", 5);
    _context.MediaFiles.Add(mediaFile);
    await _context.SaveChangesAsync();
    
    // Act
    var result = await _handler.Handle(new AbortUploadCommand(mediaFile.Id), CancellationToken.None);
    
    // Assert
    result.IsSuccess.Should().BeTrue();
    _s3Mock.Verify(x => x.AbortMultipartUploadAsync(mediaFile.Id.ToString(), "upload-id", It.IsAny<CancellationToken>()));
    var file = await _context.MediaFiles.IgnoreQueryFilters().FirstAsync(f => f.Id == mediaFile.Id);
    file.IsDeleted.Should().BeTrue();
}
```

#### CompleteMultipartUploadHandler Unit Tests  
- **For**: CompleteMultipartUpload.cs:CompleteMultipartUploadHandler.Handle
- **Type**: Unit
- **Why**: Complex multipart completion logic with S3 stitching, hash validation, and virus scanning
- **Skeleton**:
```csharp
[Fact]
public async Task Handle_ValidParts_ShouldCompleteUploadAndStartScanning()
{
    // Arrange
    var mediaFile = MediaFile.Create("test.mp4", TestFileHash, 50_000_000, "video/mp4", "owner");
    mediaFile.InitiateMultipart("upload-id", 2);
    _context.MediaFiles.Add(mediaFile);
    await _context.SaveChangesAsync();
    
    var parts = new[] { new PartETagDto(1, "etag1"), new PartETagDto(2, "etag2") };
    
    // Act
    var result = await _handler.Handle(new CompleteMultipartUploadCommand(mediaFile.Id, parts), CancellationToken.None);
    
    // Assert
    result.IsSuccess.Should().BeTrue();
    _s3Mock.Verify(x => x.CompleteMultipartUploadAsync(It.IsAny<string>(), "upload-id", It.IsAny<IList<PartETag>>(), It.IsAny<CancellationToken>()));
}
```

#### ClamAvScanner Unit Tests
- **For**: ClamAvScanner.cs:ScanAsync and ScanFileAsync
- **Type**: Unit  
- **Why**: Critical security component - virus scanning failures could allow malware uploads
- **Skeleton**:
```csharp
[Fact]
public async Task ScanAsync_CleanFile_ShouldReturnTrue()
{
    // Arrange
    _clamClientMock.Setup(x => x.SendAndScanFileAsync(It.IsAny<Stream>(), It.IsAny<CancellationToken>()))
        .ReturnsAsync(new ClamScanResult { Result = ClamScanResults.Clean });
    
    using var stream = new MemoryStream(Encoding.UTF8.GetBytes("clean content"));
    
    // Act
    var result = await _scanner.ScanAsync(stream);
    
    // Assert
    result.Should().BeTrue();
}

[Fact]
public async Task ScanAsync_InfectedFile_ShouldReturnFalse()
{
    // Arrange
    _clamClientMock.Setup(x => x.SendAndScanFileAsync(It.IsAny<Stream>(), It.IsAny<CancellationToken>()))
        .ReturnsAsync(new ClamScanResult { Result = ClamScanResults.VirusDetected });
    
    // Act & Assert
    var result = await _scanner.ScanAsync(new MemoryStream());
    result.Should().BeFalse();
}
```

#### FileSignatureValidator Unit Tests
- **For**: FileSignatureValidator.cs:ValidateAsync  
- **Type**: Unit
- **Why**: Security validation to prevent executable uploads and MIME type spoofing
- **Skeleton**:
```csharp
[Fact]
public async Task ValidateAsync_ValidPngBytes_ShouldReturnValid()
{
    // Arrange - PNG magic bytes
    var pngBytes = new byte[] { 0x89, 0x50, 0x4E, 0x47, 0x0D, 0x0A, 0x1A, 0x0A };
    
    // Act
    var result = await _validator.ValidateAsync(new MemoryStream(pngBytes));
    
    // Assert
    result.IsValid.Should().BeTrue();
    result.FileType.Should().Be("image/png");
}

[Fact]
public async Task ValidateAsync_ExecutableBytes_ShouldReturnInvalid()
{
    // Arrange - MZ header (executable)
    var exeBytes = new byte[] { 0x4D, 0x5A };
    
    // Act
    var result = await _validator.ValidateAsync(new MemoryStream(exeBytes));
    
    // Assert
    result.IsValid.Should().BeFalse();
}
```

### MEDIUM PRIORITY: Consumer Integration Tests

#### ProcessMediaConsumer Integration Tests
- **For**: ProcessMediaConsumer.cs:Consume
- **Type**: Integration
- **Why**: Async processing pipeline must handle media processing commands correctly
- **Skeleton**:
```csharp
[Fact]
public async Task Consume_ProcessMediaCommand_ShouldTriggerProcessingPipeline()
{
    // Arrange
    var mediaFile = MediaFile.Create("test.png", "hash", 1024, "image/png", "owner");
    mediaFile.MarkAsQuarantined();
    mediaFile.MarkAsActive();
    await _context.MediaFiles.AddAsync(mediaFile);
    await _context.SaveChangesAsync();
    
    var command = new ProcessMediaCommand
    {
        MediaId = mediaFile.Id,
        OwnerId = "owner",
        FileName = "test.png",
        MimeType = "image/png",
        S3Key = mediaFile.Id.ToString()
    };
    
    // Act
    await _consumer.Consume(_mockContext.Object);
    
    // Assert - verify processing orchestrator was called
    // Mock setup needed for orchestrator
}
```

#### Media Processing Pipeline Integration Tests  
- **For**: ImageProcessor.cs, VideoProcessor.cs, AudioProcessor.cs
- **Type**: Integration
- **Why**: Complex processing logic with external dependencies (FFmpeg, ImageSharp)
- **Skeleton**:
```csharp
[Fact]
public async Task ProcessAsync_ValidImage_ShouldGenerateThumbnails()
{
    // Arrange
    var mediaId = Guid.NewGuid();
    var imageBytes = GenerateTestPngBytes();
    await _s3.UploadAsync(mediaId.ToString(), "image/png", new MemoryStream(imageBytes));
    
    // Act
    var variants = await _processor.ProcessAsync(mediaId, mediaId.ToString(), "image/png", CancellationToken.None);
    
    // Assert
    variants.Should().NotBeEmpty();
    variants.Should().Contain(v => v.Kind.StartsWith("thumbnail-"));
}
```

### LOW PRIORITY: Edge Cases and Validators

#### Domain Logic Edge Cases
- **For**: MediaFile.cs business rules
- **Type**: Unit
- **Why**: Domain invariants must be enforced consistently
- **Skeleton**:
```csharp
[Fact]
public void InitiateMultipart_FromNonPendingState_ShouldThrow()
{
    // Arrange
    var file = MediaFile.Create("test", "hash", 1024, "image/png", "owner");
    file.MarkAsQuarantined();
    
    // Act & Assert
    var act = () => file.InitiateMultipart("upload-id", 5);
    act.Should().Throw<InvalidOperationException>();
}

[Fact]
public void Create_InvalidFileName_ShouldSanitize()
{
    // Arrange & Act
    var file = MediaFile.Create("../../../etc/passwd", "hash", 1024, "text/plain", "owner");
    
    // Assert  
    file.FileName.Should().NotContain("..");
    file.FileName.Should().Be("passwd");
}
```

## Test Optimization Violations

### MEDIUM: Missing SharedTestPostgres Usage in Unit Tests
- **File**: tests/Media/Media.Unit/*Tests.cs
- **Violation**: Using UseInMemoryDatabase instead of SharedTestPostgres for tests that need database
- **Fix**: Replace InMemoryDatabase with SharedTestPostgres.CreateDatabaseAsync("media") in tests that use MediaDbContext

### LOW: Missing MigrateAsync in Integration Factory  
- **File**: tests/Media/Media.Integration/MediaWebAppFactory.cs:EnsureSchemaAsync
- **Violation**: Uses MigrateAsync correctly, but drops outbox tables manually 
- **Fix**: This is actually correct for test environment since it uses in-memory harness

## E2E Coverage Gaps

### MISSING: End-to-End Media Upload Flow
- Complete upload journey: Register → Get JWT → InitiateUpload → PUT to S3 → Complete → Virus scan → Processing → GetMediaUrl → Download
- Service boundary validation (BFF → Media → S3 → ClamAV)
- Real S3/LocalStack integration with presigned URL validation

### MISSING: Media Processing E2E
- Upload large video → Transcode to HLS → Verify segments in S3 → Stream via presigned URLs  
- Image upload → Thumbnail generation → WebP conversion → Variant retrieval

## Summary

- **Total public methods**: ~40, **covered**: ~12 (30%)
- **API endpoints tested**: 8/10 (80%) 
- **Consumer handlers tested**: 0/6 (0%)
- **E2E flows tested**: 2/5 (40%)
- **Test optimization violations**: 1
- **Critical gaps**: 12

**Immediate Actions Needed**:
1. Add unit tests for all missing application handlers (AbortUpload, CompleteMultipart, DeleteMedia)
2. Add comprehensive virus scanner and file signature validator tests
3. Add consumer integration tests for the async processing pipeline  
4. Add media processing pipeline integration tests
5. Create E2E test covering complete upload→process→download flow

The integration tests in `MediaFlowsTests.cs` provide good coverage of the happy path, but the service lacks sufficient unit test coverage for error scenarios and edge cases, particularly around security-critical components like virus scanning and file validation.
