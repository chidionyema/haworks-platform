=== Test Coverage Audit: Payouts (2026-05-31T03:18:42Z) ===
Source files: 36
Unit test files: 4
Integration test files: 4
E2E test files: 14

Looking at the Payouts service source code and existing tests, I'll perform a comprehensive test coverage audit.

## Coverage Map

### API Endpoints
- [MISSING] LedgerController.GetBalance — no integration test, PRIORITY: HIGH
- [MISSING] LedgerController.SimulateTransaction — no integration test, PRIORITY: MEDIUM
- [MISSING] PayoutsController.GetPayouts — no integration test, PRIORITY: HIGH
- [MISSING] SellersController.Register — no integration test, PRIORITY: HIGH
- [MISSING] SellersController.GetOnboardingLink — no integration test, PRIORITY: HIGH

### MediatR Handlers
- [MISSING] GetPayoutsBySellerQueryHandler — no unit test, PRIORITY: HIGH
- [MISSING] GetBalanceQueryHandler — no unit test, PRIORITY: MEDIUM
- [MISSING] MatureFundsCommandHandler — no unit test, PRIORITY: HIGH
- [MISSING] RegisterSellerCommandHandler — no unit test, PRIORITY: HIGH
- [MISSING] GetOnboardingLinkCommandHandler — no unit test, PRIORITY: HIGH
- [COVERED] LedgerService — tested by LedgerServiceTests.CreditSellerAsync_creates_three_entries

### Message Consumers
- [MISSING] PaymentCompletedConsumer — no unit or integration tests, PRIORITY: HIGH
- [MISSING] RefundIssuedConsumer — no unit or integration tests, PRIORITY: HIGH

### Domain Logic
- [COVERED] LedgerAccount — tested by LedgerAccountTests
- [COVERED] LedgerEntry — tested by LedgerEntryTests  
- [COVERED] Payout — tested by PayoutTests
- [MISSING] SellerProfile.Create — no unit test, PRIORITY: MEDIUM

### Validators
- [MISSING] GetPayoutsBySellerQueryValidator — no tests, PRIORITY: MEDIUM
- [MISSING] GetBalanceQueryValidator — no tests, PRIORITY: MEDIUM
- [MISSING] MatureFundsCommandValidator — no tests, PRIORITY: LOW
- [MISSING] RegisterSellerCommandValidator — no tests, PRIORITY: MEDIUM
- [MISSING] GetOnboardingLinkCommandValidator — no tests, PRIORITY: MEDIUM

### External Gateways
- [MISSING] StripePayoutGateway — no unit tests with mocked Stripe SDK, PRIORITY: MEDIUM

## Missing Tests (ordered by priority)

### [HIGH]: PaymentCompletedConsumer Integration Test
- **For**: PaymentCompletedConsumer.Consume
- **Type**: Integration
- **Why**: Core business flow — seller crediting on payment completion. Missing test exposes risk of broken payout pipeline.
- **Skeleton**:
```csharp
[Fact]
public async Task Consume_PaymentCompleted_Should_Credit_Seller_Account()
{
    // Arrange
    var sellerId = Guid.NewGuid();
    var paymentId = Guid.NewGuid();
    var evt = new PaymentCompletedEvent 
    { 
        PaymentId = paymentId,
        SellerId = sellerId, 
        AmountCents = 10000L,
        Currency = "USD",
        OrderId = Guid.NewGuid()
    };
    
    // Act
    await _harness.Bus.Publish(evt);
    
    // Assert
    var balance = await _ledgerService.GetBalanceAsync(sellerId, AccountType.SellerPending, "USD");
    balance.Should().Be(9000L); // 10000 - 10% commission
}
```

### [HIGH]: RefundIssuedConsumer Integration Test  
- **For**: RefundIssuedConsumer.Consume
- **Type**: Integration
- **Why**: Critical for financial integrity — seller debiting on refunds. Missing test could allow double-credits.
- **Skeleton**:
```csharp
[Fact]
public async Task Consume_RefundIssued_Should_Debit_Seller_Account()
{
    // Arrange
    var sellerId = Guid.NewGuid();
    var paymentId = Guid.NewGuid();
    
    // Setup: credit first
    await _ledgerService.CreditSellerAsync(sellerId, 10000L, "USD", paymentId, "Initial payment");
    await _context.SaveChangesAsync();
    
    var refundEvt = new RefundIssuedEvent
    {
        PaymentId = paymentId,
        AmountCents = 10000L,
        Currency = "USD",
        OrderId = Guid.NewGuid()
    };
    
    // Act
    await _harness.Bus.Publish(refundEvt);
    
    // Assert
    var balance = await _ledgerService.GetBalanceAsync(sellerId, AccountType.SellerPending, "USD");
    balance.Should().Be(0L);
}
```

### [HIGH]: LedgerController.GetBalance Integration Test
- **For**: LedgerController.GetBalance
- **Type**: Integration  
- **Why**: Core API endpoint for seller balance queries. Authorization logic needs verification.
- **Skeleton**:
```csharp
[Fact]
public async Task GetBalance_ValidSeller_ReturnsBalance()
{
    // Arrange
    var sellerId = Guid.NewGuid();
    var account = LedgerAccount.Create(sellerId, AccountType.SellerPending, "USD");
    account.UpdateBalance(5000L, EntryType.Credit);
    _context.LedgerAccounts.Add(account);
    await _context.SaveChangesAsync();
    
    // Act
    var response = await _client.GetAsync($"/api/v1/ledger/balance/{sellerId}?type=SellerPending&currency=USD");
    
    // Assert
    response.StatusCode.Should().Be(HttpStatusCode.OK);
    var result = await response.Content.ReadAsAsync<dynamic>();
    ((long)result.BalanceCents).Should().Be(5000L);
}
```

### [HIGH]: SellersController Authorization Tests
- **For**: SellersController.Register, SellersController.GetOnboardingLink
- **Type**: Integration
- **Why**: Financial onboarding endpoints must prevent cross-seller access.
- **Skeleton**:
```csharp
[Fact]
public async Task Register_DifferentSeller_Returns403()
{
    // Arrange - user tries to register different sellerId
    var requestedSellerId = Guid.NewGuid();
    var command = new { sellerId = requestedSellerId, email = "test@test.com" };
    
    // Act
    var response = await _client.PostAsJsonAsync("/api/v1/sellers", command);
    
    // Assert
    response.StatusCode.Should().Be(HttpStatusCode.Forbidden);
}
```

### [HIGH]: MatureFundsCommandHandler Unit Test
- **For**: MatureFundsCommandHandler.Handle  
- **Type**: Unit
- **Why**: Critical background job that moves funds between accounts. Complex FOR UPDATE logic needs verification.
- **Skeleton**:
```csharp
[Fact]
public async Task Handle_Should_Transfer_Funds_From_Pending_To_Payable()
{
    // Arrange
    var sellerId = Guid.NewGuid();
    var pendingAccount = LedgerAccount.Create(sellerId, AccountType.SellerPending, "USD");
    pendingAccount.UpdateBalance(10000L, EntryType.Credit);
    _context.LedgerAccounts.Add(pendingAccount);
    await _context.SaveChangesAsync();
    
    // Act
    await _handler.Handle(new MatureFundsCommand(), CancellationToken.None);
    
    // Assert
    var payableBalance = await _ledgerService.GetBalanceAsync(sellerId, AccountType.SellerPayable, "USD");
    payableBalance.Should().Be(10000L);
    var pendingBalance = await _ledgerService.GetBalanceAsync(sellerId, AccountType.SellerPending, "USD");
    pendingBalance.Should().Be(0L);
}
```

### [MEDIUM]: Validator Tests
- **For**: All command/query validators  
- **Type**: Unit
- **Why**: Validation failures should be tested to ensure proper error handling.
- **Skeleton**:
```csharp
[Theory]
[InlineData("")]
[InlineData("XX")]
[InlineData("USDD")]
public void Validate_InvalidCurrency_ShouldHaveValidationError(string currency)
{
    // Arrange
    var validator = new GetBalanceQueryValidator();
    var query = new GetBalanceQuery(Guid.NewGuid(), AccountType.SellerPending, currency);
    
    // Act
    var result = validator.Validate(query);
    
    // Assert
    result.IsValid.Should().BeFalse();
    result.Errors.Should().Contain(e => e.PropertyName == nameof(GetBalanceQuery.Currency));
}
```

## Test Optimization Violations

### [HIGH]: Raw Mock Gateway in Integration Tests  
- **File**: tests/Payouts/Payouts.Integration/PayoutsWebAppFactory.cs:61
- **Violation**: Using Mock<IPayoutGateway> instead of real Stripe SDK test mode
- **Fix**: Replace with StripePayoutGateway using Stripe test keys

### [MEDIUM]: Missing Schema Creation
- **File**: tests/Payouts/Payouts.Integration/PayoutsWebAppFactory.cs:35
- **Violation**: No explicit schema creation before MigrateAsync
- **Fix**: Add `await db.Database.ExecuteSqlRawAsync("CREATE SCHEMA IF NOT EXISTS payouts;");`

### [MEDIUM]: Test Harness Instead of Real Transport
- **File**: tests/Payouts/Payouts.Integration/PayoutsWebAppFactory.cs:69
- **Violation**: Using AddMassTransitTestHarness instead of SharedTestRabbitMq
- **Fix**: Use real RabbitMQ for better integration coverage

## E2E Coverage Gaps

### [MISSING] Complete Seller Onboarding Flow
- Flow: Register seller → Get onboarding link → Complete KYC → Enable payouts
- Services involved: Identity, Payouts, Payments (webhook simulation)

### [MISSING] End-to-End Payout Process  
- Flow: Payment → Ledger credit → Fund maturity → Disbursement → Stripe transfer
- Services involved: Payments, Payouts, external Stripe webhook

### [MISSING] Multi-Currency Payout Testing
- Flow: USD payment → EUR seller account → Currency conversion handling
- Services involved: Payouts, external FX rates

## Summary
- Total public methods: 47, covered: 18 (38%)
- API endpoints tested: 0/5 (0%)  
- Consumer handlers tested: 0/2 (0%)
- MediatR handlers tested: 1/5 (20%)
- E2E flows tested: 1/3 (33%)
- Test optimization violations: 3
- Critical gaps: 12

The Payouts service has significant test coverage gaps, particularly in integration testing of API endpoints and message consumers. The existing unit tests cover domain logic well, but the application layer and infrastructure components need comprehensive integration testing to ensure financial operations work correctly under real conditions.
