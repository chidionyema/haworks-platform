=== Test Coverage Audit: Identity (2026-05-31T09:50:23Z) ===
Source files: 80
Unit test files: 23
Integration test files: 3
E2E test files: 14

I'll perform a comprehensive test coverage audit of the Identity service. Let me analyze the source files against the existing tests to identify gaps.

## Coverage Map

### Controllers
- [COVERED] AuthenticationController basic flows — tested by AuthFlowsTests, RoundedOutAuthFlowsTests
- [MISSING] AdminController — no test found, PRIORITY: HIGH
- [PARTIAL] ExternalAuthenticationController — challenge tested, missing: callback, link/unlink, logins
- [COVERED] JwksController — tested by AuthFlowsTests  
- [COVERED] UserProfileController — tested by UserProfileControllerTests

### Application Commands
- [COVERED] LoginCommand — tested by LoginCommandHandlerTests + integration
- [COVERED] RegisterCommand — tested by RegisterCommandHandlerTests + integration
- [COVERED] RefreshTokenCommand — tested by RefreshTokenCommandHandlerTests + integration
- [MISSING] ExternalLoginCallbackCommand — no unit test found, PRIORITY: HIGH
- [MISSING] LinkExternalLoginCommand — no unit test found, PRIORITY: MEDIUM
- [MISSING] UnlinkExternalLoginCommand — no unit test found, PRIORITY: MEDIUM
- [COVERED] SaveShippingInfoCommand — tested by SaveShippingInfoCommandHandlerTests
- [COVERED] UpdateUserProfileCommand — tested by UpdateUserProfileCommandHandlerTests

### Application Queries  
- [COVERED] VerifyTokenQuery — tested by VerifyTokenQueryHandlerTests
- [COVERED] GetUserProfileQuery — tested by GetUserProfileQueryHandlerTests
- [COVERED] GetAvailableProvidersQuery — tested by GetAvailableProvidersQueryHandlerTests
- [MISSING] GetUserLoginsQuery — no unit test found, PRIORITY: MEDIUM

### Domain Entities
- [COVERED] User — tested by UserTests
- [COVERED] UserProfile — tested by UserProfileTests  
- [MISSING] RefreshToken — domain logic untested, PRIORITY: MEDIUM
- [MISSING] RevokedToken — domain logic untested, PRIORITY: LOW

### Infrastructure Services
- [COVERED] JwtTokenService — tested by JwtTokenServiceTests
- [COVERED] RefreshTokenService — tested by RefreshTokenServiceTests
- [COVERED] TokenRevocationService — tested by TokenRevocationServiceTests
- [COVERED] UserEmailService — tested by UserEmailServiceTests
- [COVERED] DualKeyJwtValidator — tested by DualKeyJwtValidatorTests

### MassTransit Consumers
- [MISSING] JwtKeyRotatedConsumer — no test found, PRIORITY: HIGH
- [MISSING] PrivacyErasureRequestedConsumer — no test found, PRIORITY: HIGH

## Missing Tests (ordered by priority)

### [HIGH]: AdminController vault endpoints unit tests
- **For**: AdminController.cs:GetVaultStatus, RotateCredentials  
- **Type**: Unit
- **Why**: Admin endpoints handle sensitive vault operations; missing coverage of vault status checks and credential rotation
- **Skeleton**:
```csharp
[Fact]
public async Task GetVaultStatus_WhenVaultEnabled_ReturnsHealthyStatus()
{
    // Arrange
    var mockVaultProbe = Mock.Of<VaultProbeClient>();
    var controller = new AdminController(serviceProvider, configuration, publishEndpoint, logger);
    
    // Act
    var result = await controller.GetVaultStatus(CancellationToken.None);
    
    // Assert
    result.Should().BeOfType<OkObjectResult>();
}
```

### [HIGH]: ExternalLoginCallbackCommand unit tests
- **For**: ExternalLoginCallbackCommand.cs:Handle
- **Type**: Unit  
- **Why**: Critical OAuth security flow lacks unit test coverage for race conditions, invalid providers, email verification
- **Skeleton**:
```csharp
[Fact]
public async Task Handle_WithValidOAuthCallback_CreatesUserAndReturnsToken()
{
    // Arrange
    var mockSignInManager = CreateMockSignInManager();
    var handler = new ExternalLoginCallbackCommandHandler(mockSignInManager, /* other deps */);
    var command = new ExternalLoginCallbackCommand(httpContext);
    
    // Act
    var result = await handler.Handle(command, CancellationToken.None);
    
    // Assert
    result.IsSuccess.Should().BeTrue();
    result.Value.Token.Should().NotBeNullOrEmpty();
}
```

### [HIGH]: JwtKeyRotatedConsumer unit tests
- **For**: JwtKeyRotatedConsumer.cs:Consume
- **Type**: Unit
- **Why**: Key rotation consumer handles security-critical JWT key updates; no test coverage found
- **Skeleton**:
```csharp
[Fact]
public async Task Consume_JwtKeyRotatedEvent_RefreshesVaultCredentials()
{
    // Arrange
    var mockVault = new Mock<IVaultService>();
    var consumer = new JwtKeyRotatedConsumer(mockVault.Object, /* other deps */);
    var evt = new JwtKeyRotatedEvent { RotationId = Guid.NewGuid() };
    
    // Act
    await consumer.Consume(Mock.Of<ConsumeContext<JwtKeyRotatedEvent>>());
    
    // Assert
    mockVault.Verify(v => v.RefreshCredentials("haworks-identity", It.IsAny<CancellationToken>()), Times.Once);
}
```

### [HIGH]: PrivacyErasureRequestedConsumer unit tests  
- **For**: PrivacyErasureRequestedConsumer.cs:Consume
- **Type**: Unit
- **Why**: GDPR compliance consumer lacks test coverage for user anonymization logic
- **Skeleton**:
```csharp
[Fact]
public async Task Consume_PrivacyErasureRequested_AnonymizesUserData()
{
    // Arrange
    var user = new User { Id = "user-123", UserName = "testuser", Email = "test@example.com" };
    var mockUserManager = CreateMockUserManager();
    mockUserManager.Setup(m => m.FindByIdAsync("user-123")).ReturnsAsync(user);
    var consumer = new PrivacyErasureRequestedConsumer(mockUserManager.Object, /* other deps */);
    
    // Act
    await consumer.Consume(Mock.Of<ConsumeContext<PrivacyErasureRequested>>());
    
    // Assert
    user.Email.Should().Contain("privacy.invalid");
    user.IsActive.Should().BeFalse();
}
```

### [MEDIUM]: RefreshToken domain entity tests
- **For**: RefreshToken.cs:Create, IsExpired
- **Type**: Unit
- **Why**: Domain entity with business logic (expiration checks) lacks direct unit tests
- **Skeleton**:
```csharp
[Fact]
public void Create_WithValidParameters_SetsPropertiesCorrectly()
{
    // Arrange & Act
    var token = RefreshToken.Create("user-123", "token-value", DateTime.UtcNow.AddDays(7));
    
    // Assert
    token.UserId.Should().Be("user-123");
    token.Token.Should().Be("token-value");
    token.IsExpired.Should().BeFalse();
}
```

### [MEDIUM]: External authentication integration tests  
- **For**: ExternalAuthenticationController.cs:LinkExternalLogin, UnlinkExternalLogin
- **Type**: Integration
- **Why**: OAuth linking/unlinking flows lack integration test coverage for full request/response cycle
- **Skeleton**:
```csharp
[Fact]
public async Task LinkExternalLogin_WithValidProvider_Returns200()
{
    // Arrange
    var client = _factory.CreateClient();
    await AuthenticateAsync(client);
    
    // Act
    var response = await client.PostAsync("/api/v1/external-authentication/link/Google", null);
    
    // Assert
    response.StatusCode.Should().Be(HttpStatusCode.OK);
}
```

### [MEDIUM]: Service token endpoint integration tests
- **For**: AuthenticationController.cs:ServiceToken  
- **Type**: Integration
- **Why**: Service-to-service authentication endpoint lacks integration test coverage
- **Skeleton**:
```csharp
[Fact]
public async Task ServiceToken_WithValidSecret_ReturnsJwt()
{
    // Arrange
    var client = _factory.CreateClient();
    client.DefaultRequestHeaders.Add("X-Service-Secret", "valid-secret");
    
    // Act
    var response = await client.PostAsync("/api/v1/Authentication/service-token", null);
    
    // Assert
    response.StatusCode.Should().Be(HttpStatusCode.OK);
    var token = await response.Content.ReadFromJsonAsync<ServiceTokenResponse>();
    token.Token.Should().NotBeNullOrEmpty();
}
```

## Test Optimization Violations

### [HIGH]: Missing integration test schema setup
- **File**: tests/Identity/Identity.Integration/IdentityWebAppFactory.cs:EnsureSchemaAsync
- **Violation**: Uses EnsureCreatedAsync instead of MigrateAsync pattern
- **Fix**: Replace with `await db.Database.MigrateAsync()` to match production migration behavior

### [MEDIUM]: Missing SharedTestPostgres usage validation
- **File**: tests/Identity/Identity.Unit/Services/RefreshTokenServiceTests.cs:CreateSqliteContext
- **Violation**: Creates SQLite context manually instead of using SharedTestPostgres singleton
- **Fix**: Replace SQLite setup with `await SharedTestPostgres.CreateDatabaseAsync("refresh-token-tests")`

## E2E Coverage Gaps

### [MISSING] OAuth provider integration flow
- **Services involved**: Identity, External OAuth providers
- **E2E tests MUST be in tests/E2E/, gated by E2E_ENABLED=1**

### [MISSING] Cross-service JWT validation flow  
- **Services involved**: Identity (issues JWT), BffWeb (validates via JWKS)
- **E2E tests MUST be in tests/E2E/, gated by E2E_ENABLED=1**

### [MISSING] Privacy erasure orchestration flow
- **Services involved**: Identity, Privacy, Audit
- **E2E tests MUST be in tests/E2E/, gated by E2E_ENABLED=1**

## Summary

- **Total public methods**: 47, **covered**: 32 (68%)
- **API endpoints tested**: 8/12  
- **Consumer handlers tested**: 0/2
- **E2E flows tested**: 2/5
- **Test optimization violations**: 2
- **Critical gaps**: 3 (AdminController, MassTransit consumers, external auth)

The Identity service has solid unit test coverage for core authentication flows but significant gaps in administrative endpoints, event consumers, and external authentication scenarios. The missing MassTransit consumer tests are particularly concerning as they handle security-critical operations (key rotation) and compliance requirements (GDPR erasure).
