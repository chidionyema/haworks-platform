=== Test Coverage Audit: Merchant (2026-05-30T18:11:45Z) ===
Source files: 28
Unit test files: 4
Integration test files: 3
E2E test files: 14

# Merchant Service Test Coverage Audit

## Coverage Map

### API Controller Coverage
- **[MISSING]** MerchantsController.Create - no test found, **PRIORITY: HIGH**
- **[MISSING]** MerchantsController.GetById - no test found, **PRIORITY: HIGH** 
- **[MISSING]** MerchantsController.GetBySlug - no test found, **PRIORITY: HIGH**
- **[MISSING]** MerchantsController.GetMine - no test found, **PRIORITY: HIGH**
- **[MISSING]** MerchantsController.List - no test found, **PRIORITY: HIGH**
- **[MISSING]** MerchantsController.Update - no test found, **PRIORITY: HIGH**
- **[MISSING]** MerchantsController.SetHours - no test found, **PRIORITY: HIGH**
- **[MISSING]** MerchantsController.Approve - no test found, **PRIORITY: HIGH**
- **[MISSING]** MerchantsController.Reject - no test found, **PRIORITY: HIGH**
- **[MISSING]** MerchantsController.Suspend - no test found, **PRIORITY: HIGH**
- **[MISSING]** MerchantsController.Deactivate - no test found, **PRIORITY: HIGH**

### Command Handlers Coverage
- **[MISSING]** ApproveMerchantCommandHandler - no test found, **PRIORITY: HIGH**
- **[MISSING]** CreateMerchantCommandHandler - no test found, **PRIORITY: HIGH**
- **[MISSING]** DeactivateMerchantCommandHandler - no test found, **PRIORITY: HIGH**
- **[MISSING]** RejectMerchantCommandHandler - no test found, **PRIORITY: HIGH**
- **[MISSING]** SetOperatingHoursCommandHandler - no test found, **PRIORITY: HIGH**
- **[MISSING]** SuspendMerchantCommandHandler - no test found, **PRIORITY: HIGH**
- **[COVERED]** UpdateMerchantCommandHandler - tested by UpdateMerchantHandlerTests.cs

### Query Handlers Coverage
- **[MISSING]** GetMerchantByIdQueryHandler - no test found, **PRIORITY: MEDIUM**
- **[MISSING]** GetMerchantByOwnerQueryHandler - no test found, **PRIORITY: MEDIUM**
- **[MISSING]** GetMerchantBySlugQueryHandler - no test found, **PRIORITY: MEDIUM**
- **[MISSING]** ListMerchantsQueryHandler - no test found, **PRIORITY: MEDIUM**

### Domain Model Coverage
- **[COVERED]** MerchantProfile.Create - tested by MerchantProfileTests.Create_sets_status_to_Pending
- **[COVERED]** MerchantProfile state transitions - comprehensive coverage in MerchantProfileTests.cs
- **[COVERED]** OperatingHours - tested by OperatingHoursTests.cs

### Validator Coverage
- **[PARTIAL]** CreateMerchantCommandValidator - happy path and basic validation in ValidatorTests.cs, missing: edge cases
- **[PARTIAL]** UpdateMerchantCommandValidator - basic validation in ValidatorTests.cs, missing: boundary cases
- **[PARTIAL]** RejectMerchantCommandValidator - basic validation in ValidatorTests.cs
- **[MISSING]** ApproveMerchantCommandValidator - no test found, **PRIORITY: MEDIUM**
- **[MISSING]** DeactivateMerchantCommandValidator - no test found, **PRIORITY: MEDIUM**
- **[MISSING]** SetOperatingHoursCommandValidator - no test found, **PRIORITY: HIGH**
- **[MISSING]** SuspendMerchantCommandValidator - no test found, **PRIORITY: MEDIUM**
- **[MISSING]** Query validators - no tests found, **PRIORITY: LOW**

## Missing Tests (ordered by priority)

### **HIGH PRIORITY**: API Endpoint Integration Tests

#### Authorization Tests
- **For**: MerchantsController.cs - all endpoints
- **Type**: Integration
- **Why**: Security vulnerability - no tests verify authorization policies work
- **Skeleton**:
```csharp
[Fact]
public async Task Approve_without_admin_role_returns_403()
{
    var factory = new MerchantWebAppFactory();
    factory.ConfigureTestUser(roles: ["User"]); // Not Admin
    var client = factory.CreateClient();
    
    var response = await client.PostAsync($"/api/v1/merchants/{Guid.NewGuid()}/approve", null);
    
    response.StatusCode.Should().Be(HttpStatusCode.Forbidden);
}
```

#### Owner Authorization Tests  
- **For**: MerchantsController.cs:Update, SetHours, Deactivate
- **Type**: Integration
- **Why**: Critical business logic - users should only modify their own merchants
- **Skeleton**:
```csharp
[Fact]
public async Task Update_other_users_merchant_returns_forbidden()
{
    var factory = new MerchantWebAppFactory();
    factory.ConfigureTestUser(userId: "user1");
    var client = factory.CreateClient();
    
    // Create merchant with different owner
    var merchant = await CreateMerchantWithOwner("user2");
    
    var response = await client.PutAsJsonAsync($"/api/v1/merchants/{merchant.Id}", 
        new { Name = "Hacked" });
    
    response.StatusCode.Should().Be(HttpStatusCode.Forbidden);
}
```

### **HIGH PRIORITY**: Command Handler Unit Tests

#### CreateMerchantCommandHandler Tests
- **For**: CreateMerchantCommand.cs:Handle
- **Type**: Unit
- **Why**: Core business flow - merchant creation with event publishing and concurrency handling
- **Skeleton**:
```csharp
[Fact]
public async Task Handle_duplicate_slug_throws_invalid_operation()
{
    var context = new TestMerchantDbContext();
    var publishEndpoint = new Mock<IPublishEndpoint>();
    var handler = new CreateMerchantCommandHandler(context, publishEndpoint.Object);
    
    // Create existing merchant with slug
    var existing = MerchantProfile.Create(Guid.NewGuid(), "First", "test-slug");
    context.Merchants.Add(existing);
    await context.SaveChangesAsync();
    
    var command = new CreateMerchantCommand(Guid.NewGuid(), "Second", "test-slug");
    
    var act = async () => await handler.Handle(command, CancellationToken.None);
    
    await act.Should().ThrowAsync<InvalidOperationException>()
        .WithMessage("*slug*already in use*");
}
```

#### SetOperatingHoursCommandHandler Tests
- **For**: SetOperatingHoursCommand.cs:Handle  
- **Type**: Unit
- **Why**: Complex business logic with validation, authorization, and batch updates
- **Skeleton**:
```csharp
[Fact]
public async Task Handle_invalid_time_ranges_fails_validation()
{
    var context = new TestMerchantDbContext();
    var handler = new SetOperatingHoursCommandHandler(context);
    
    var hours = new List<OperatingHourDto> 
    {
        new(DayOfWeek.Monday, TimeSpan.FromHours(17), TimeSpan.FromHours(9), true) // Close before open
    };
    var command = new SetOperatingHoursCommand(Guid.NewGuid(), Guid.NewGuid(), hours);
    
    var result = await handler.Handle(command, CancellationToken.None);
    
    result.IsFailure.Should().BeTrue();
}
```

### **HIGH PRIORITY**: Event Publishing Tests

#### Event Integration Tests
- **For**: Command handlers that publish events
- **Type**: Integration
- **Why**: Event-driven architecture correctness - missing events break downstream services
- **Skeleton**:
```csharp
[Fact]
public async Task Approve_merchant_publishes_activated_event()
{
    var harness = _factory.Services.GetRequiredService<ITestHarness>();
    var client = _factory.CreateClient();
    
    var merchant = await CreatePendingMerchant();
    
    var response = await client.PostAsync($"/api/v1/merchants/{merchant.Id}/approve", null);
    
    response.StatusCode.Should().Be(HttpStatusCode.OK);
    (await harness.Published.Any<MerchantActivatedEvent>(
        x => x.Context.Message.MerchantId == merchant.Id)).Should().BeTrue();
}
```

### **MEDIUM PRIORITY**: Query Handler Tests

#### Database Query Tests
- **For**: All query handlers
- **Type**: Unit  
- **Why**: Data integrity - queries might return incorrect results or fail under edge conditions
- **Skeleton**:
```csharp
[Fact]
public async Task GetById_nonexistent_id_returns_not_found()
{
    var context = new TestMerchantDbContext();
    var handler = new GetMerchantByIdQueryHandler(context);
    
    var result = await handler.Handle(new GetMerchantByIdQuery(Guid.NewGuid()), CancellationToken.None);
    
    result.IsFailure.Should().BeTrue();
    result.Error.Code.Should().Be("Merchant.NotFound");
}
```

### **MEDIUM PRIORITY**: Validator Edge Cases

#### Complex Validation Rules
- **For**: SetOperatingHoursCommandValidator.cs
- **Type**: Unit
- **Why**: Business rule enforcement - invalid data could corrupt merchant schedules
- **Skeleton**:
```csharp
[Fact]
public void SetOperatingHours_duplicate_days_fails()
{
    var validator = new SetOperatingHoursCommandValidator();
    var hours = new List<OperatingHourDto>
    {
        new(DayOfWeek.Monday, TimeSpan.FromHours(9), TimeSpan.FromHours(17), true),
        new(DayOfWeek.Monday, TimeSpan.FromHours(18), TimeSpan.FromHours(22), true) // Duplicate Monday
    };
    var command = new SetOperatingHoursCommand(Guid.NewGuid(), Guid.NewGuid(), hours);
    
    var result = validator.TestValidate(command);
    
    result.ShouldHaveValidationErrorFor(c => c.Hours)
        .WithMessage("Duplicate DayOfWeek entries are not allowed.");
}
```

### **LOW PRIORITY**: Negative Test Cases

#### Error Handling Tests
- **For**: All command handlers
- **Type**: Unit
- **Why**: Resilience - uncaught exceptions could bring down the service
- **Skeleton**:
```csharp
[Fact]
public async Task Handle_database_save_fails_throws_exception()
{
    var mockContext = new Mock<IMerchantDbContext>();
    mockContext.Setup(c => c.SaveChangesAsync(It.IsAny<CancellationToken>()))
        .ThrowsAsync(new DbUpdateException());
    var handler = new CreateMerchantCommandHandler(mockContext.Object, Mock.Of<IPublishEndpoint>());
    
    var command = new CreateMerchantCommand(Guid.NewGuid(), "Test", "test");
    
    var act = async () => await handler.Handle(command, CancellationToken.None);
    
    await act.Should().ThrowAsync<DbUpdateException>();
}
```

## Test Optimization Violations

### **MEDIUM**: Test Factory Setup Issues
- **File**: tests/Merchant/Merchant.Integration/MerchantWebAppFactory.cs:line N/A
- **Violation**: Missing verification of MigrationHostedService or MigrationStartupFilter
- **Fix**: Add explicit migration handling:
```csharp
public async Task EnsureSchemaAsync()
{
    using var scope = Services.CreateScope();
    var db = scope.ServiceProvider.GetRequiredService<MerchantDbContext>();
    
    // Create schema before migrate
    await db.Database.ExecuteSqlRawAsync("CREATE SCHEMA IF NOT EXISTS merchant;");
    await db.Database.MigrateAsync();
}
```

### **LOW**: Test Context Implementation  
- **File**: tests/Merchant/Merchant.Unit/Application/UpdateMerchantHandlerTests.cs:line 68
- **Violation**: Uses in-memory database instead of real Postgres
- **Fix**: Use SharedTestPostgres for realistic testing:
```csharp
private static async Task<TestMerchantDbContext> CreateTestContextAsync()
{
    var connectionString = await SharedTestPostgres.CreateDatabaseAsync("merchant");
    var options = new DbContextOptionsBuilder<TestMerchantDbContext>()
        .UseNpgsql(connectionString)
        .Options;
    var context = new TestMerchantDbContext(options);
    await context.Database.MigrateAsync();
    return context;
}
```

## E2E Coverage Gaps

### **MISSING**: Complete Merchant Lifecycle
- **Flow**: Register user → Create merchant → Admin review → Approval/Rejection → Updates → Deactivation
- **Services**: Identity + Merchant + potential downstream consumers
- **Why**: End-to-end business process validation

### **MISSING**: Cross-Service Event Propagation  
- **Flow**: MerchantActivatedEvent → downstream service reactions (e.g., Search indexing, Audit logging)
- **Services**: Merchant → Search/Audit/etc.
- **Why**: Event-driven architecture verification

## Summary

- **Total public methods**: 47, **covered**: 8 (17%)
- **API endpoints tested**: 1/11 (9%) 
- **Command handlers tested**: 1/6 (17%)
- **Query handlers tested**: 0/4 (0%)
- **E2E flows tested**: 1/3 (33%)
- **Test optimization violations**: 2
- **Critical gaps**: 31

**Most Critical Missing Coverage:**
1. **API authorization and validation** - Security risk
2. **Command handler business logic** - Data corruption risk  
3. **Event publishing verification** - Integration failure risk
4. **Concurrency and error handling** - Reliability risk

The service has good domain model coverage but lacks comprehensive application and API layer testing. Integration tests are minimal and E2E coverage is basic.
