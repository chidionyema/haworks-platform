=== Test Coverage Audit: Catalog (2026-05-31T13:13:03Z) ===
Source files: 90
Unit test files: 16
Integration test files: 8
E2E test files: 14

Looking at the Catalog service source and test files, I'll perform a comprehensive test coverage audit.

## Coverage Map

### API Controllers

#### [COVERED] CategoriesController
- **POST /api/v1/categories** — tested by CatalogFlowsTests.Create_then_get_category_round_trips
- **GET /api/v1/categories** — tested by CatalogFlowsTests.Create_then_get_category_round_trips

#### [MISSING] ProductsController - Partial Coverage
- [COVERED] **GET /api/v1/products** — tested by CatalogFlowsTests.Create_then_list_product_round_trips
- [COVERED] **GET /api/v1/products/{id}** — tested by CatalogFlowsTests.Get_missing_product_returns_404
- [COVERED] **POST /api/v1/products** — tested by CatalogFlowsTests.Create_then_list_product_round_trips
- [MISSING] **PUT /api/v1/products/{id}** — no integration test, PRIORITY: HIGH
- [MISSING] **DELETE /api/v1/products/{id}** — no integration test, PRIORITY: HIGH
- [MISSING] **GET /api/v1/products/{id}/cached** — no integration test, PRIORITY: MEDIUM
- [COVERED] **POST /api/v1/products/{id}/reserve** — tested by CatalogFlowsTests.Reserve_stock_decrements_and_publishes_event

#### [MISSING] ProductReviewsController - Critical Gap
- [PARTIAL] **POST /api/v1/products/{id}/reviews** — only auth test in ProductReviewTests.CreateReview_returns_201_when_authenticated_and_header_present
- [MISSING] **GET /api/v1/products/{id}/reviews** — no test found, PRIORITY: HIGH
- [MISSING] **GET /api/v1/products/{id}/reviews/{reviewId}** — no test found, PRIORITY: HIGH  
- [MISSING] **PUT /api/v1/products/{id}/reviews/{reviewId}** — no test found, PRIORITY: HIGH
- [MISSING] **DELETE /api/v1/products/{id}/reviews/{reviewId}** — no test found, PRIORITY: HIGH
- [MISSING] **POST /api/v1/products/{id}/reviews/{reviewId}/approve** — no test found, PRIORITY: HIGH

#### [MISSING] ReservationsController - Partial Coverage  
- [COVERED] **POST /api/v1/checkout/reservations** — tested by ReservationEndpointTests
- [COVERED] **POST /api/v1/checkout/reservations/{id}/confirm** — tested by ReservationEndpointTests

#### [MISSING] DemoConcurrencyController - No Coverage
- [MISSING] **GET /demo/inventory/{productId}** — no test found, PRIORITY: MEDIUM
- [MISSING] **PUT /demo/inventory/{productId}** — no test found, PRIORITY: HIGH

#### [MISSING] DemoTestController - No Coverage  
- [MISSING] **GET /demo/fail** — no test found, PRIORITY: LOW
- [MISSING] **POST /demo/chaos/trigger** — no test found, PRIORITY: LOW
- [MISSING] **GET /demo/health-with-chaos** — no test found, PRIORITY: LOW
- [MISSING] **POST /demo/cache-stampede** — no test found, PRIORITY: MEDIUM
- [MISSING] **POST /demo/cache/seed-demo-product** — no test found, PRIORITY: LOW

### Domain Layer

#### [COVERED] Product.cs
- **Basic operations** — tested by ProductTests (Domain/ProductTests.cs)
- **Stock management** — tested by ProductTests.ReserveStock/ReleaseStock methods

#### [MISSING] Category.cs - No Domain Tests
- [MISSING] **Create factory method** — no test found, PRIORITY: HIGH
- [MISSING] **Rename method** — no test found, PRIORITY: HIGH

#### [MISSING] ProductReview.cs - No Domain Tests
- [MISSING] **Create factory method** — no test found, PRIORITY: HIGH
- [MISSING] **Approve/Reject methods** — no test found, PRIORITY: HIGH
- [MISSING] **Update method** — no test found, PRIORITY: HIGH

#### [COVERED] StockReservation.cs
- **Lifecycle transitions** — tested by StockReservationTests

#### [MISSING] ProductMetadata.cs - No Tests
- [MISSING] **Create/UpdateValue methods** — no test found, PRIORITY: MEDIUM

#### [MISSING] ProductSpecification.cs - No Tests
- [MISSING] **Create/Update methods** — no test found, PRIORITY: MEDIUM

### Application Layer - Command Handlers

#### [MISSING] CreateCategoryCommandHandler
- [MISSING] **Valid category creation** — no test found, PRIORITY: HIGH
- [MISSING] **Duplicate name handling** — no test found, PRIORITY: MEDIUM

#### [MISSING] CreateProductCommandHandler  
- [MISSING] **Valid product creation** — no test found, PRIORITY: HIGH
- [MISSING] **Invalid category handling** — no test found, PRIORITY: HIGH
- [MISSING] **Stock initialization** — no test found, PRIORITY: HIGH

#### [COVERED] UpdateProductCommandHandler
- **Valid updates** — tested by UpdateProductCommandHandlerTests

#### [COVERED] DeleteProductCommandHandler
- **Valid deletion** — tested by DeleteProductCommandHandlerTests

#### [MISSING] Product Review Command Handlers
- [MISSING] **CreateProductReviewCommandHandler** — no unit test, PRIORITY: HIGH
- [MISSING] **UpdateProductReviewCommandHandler** — no unit test, PRIORITY: HIGH  
- [MISSING] **DeleteProductReviewCommandHandler** — no unit test, PRIORITY: HIGH
- [MISSING] **ApproveProductReviewCommandHandler** — no unit test, PRIORITY: HIGH

#### [MISSING] ReserveStockCommandHandler
- [MISSING] **Successful reservation** — no unit test, PRIORITY: HIGH
- [MISSING] **Insufficient stock handling** — no unit test, PRIORITY: HIGH
- [MISSING] **Concurrency conflict handling** — no unit test, PRIORITY: HIGH

### Application Layer - Consumers

#### [MISSING] StockReservationRequestedConsumer - Critical Gap
- [MISSING] **Happy path stock reservation** — no unit test, PRIORITY: HIGH
- [MISSING] **Insufficient stock handling** — no unit test, PRIORITY: HIGH  
- [MISSING] **Idempotency behavior** — no unit test, PRIORITY: HIGH
- [MISSING] **Event publication verification** — no unit test, PRIORITY: HIGH

#### [PARTIAL] StockReleaseRequestedConsumer
- [COVERED] **Retry behavior** — tested by StockReleaseRetryHarnessTests
- [MISSING] **Happy path stock release** — no unit test, PRIORITY: HIGH
- [MISSING] **Idempotency behavior** — no unit test, PRIORITY: HIGH

#### [COVERED] StockReleaseFaultConsumer
- **Fault handling** — tested by StockReleaseFaultConsumerTests

### Infrastructure Layer

#### [MISSING] ProductCacheReader - No Tests
- [MISSING] **Cache hit/miss behavior** — no test found, PRIORITY: HIGH
- [MISSING] **Invalidation logic** — no test found, PRIORITY: HIGH

#### [PARTIAL] ReservationSweeperService
- [COVERED] **Sweep behavior** — tested by ReservationSweeperTests (integration)
- [MISSING] **Background service lifecycle** — no unit test, PRIORITY: MEDIUM

#### [MISSING] StockService - No Tests
- [MISSING] **ReserveStockAsync** — no test found, PRIORITY: HIGH
- [MISSING] **ReleaseStockAsync** — no test found, PRIORITY: HIGH
- [MISSING] **Transaction behavior** — no test found, PRIORITY: HIGH

## Missing Tests (Ordered by Priority)

### [HIGH]: Product Review Integration Tests
**For**: ProductReviewsController.cs:all_endpoints  
**Type**: Integration
**Why**: Product reviews are a core feature with complex authorization rules (user ownership vs admin) and no integration coverage
**Skeleton**:
```csharp
[Fact]
public async Task GetProductReviews_Returns_OnlyApproved_ForNonAdmin()
{
    // Arrange
    var (categoryId, productId) = await CreateProductAsync();
    var approvedReviewId = await CreateApprovedReviewAsync(productId);
    var unapprovedReviewId = await CreateUnapprovedReviewAsync(productId);
    
    // Act
    var response = await _client.GetAsync($"/api/v1/products/{productId}/reviews");
    
    // Assert
    response.StatusCode.Should().Be(HttpStatusCode.OK);
    var reviews = await response.Content.ReadFromJsonAsync<ProductReviewResponse[]>();
    reviews.Should().ContainSingle(r => r.Id == approvedReviewId);
    reviews.Should().NotContain(r => r.Id == unapprovedReviewId);
}
```

### [HIGH]: StockReservationRequestedConsumer Unit Tests
**For**: StockReservationRequestedConsumer.cs:Consume
**Type**: Unit  
**Why**: Critical consumer for saga choreography with complex idempotency and event publication logic
**Skeleton**:
```csharp
[Fact]
public async Task Consume_PublishesStockReservedEvent_WhenSuccessful()
{
    // Arrange
    var products = new Mock<IProductRepository>();
    var product = Product.Create("Test", "Desc", 1000L, "USD", Guid.NewGuid());
    product.RestockTo(10);
    products.Setup(x => x.GetByIdTrackedAsync(It.IsAny<Guid>(), It.IsAny<CancellationToken>()))
            .ReturnsAsync(product);
    
    var publishEndpoint = new Mock<IPublishEndpoint>();
    var consumer = new StockReservationRequestedConsumer(products.Object, Mock.Of<ILogger<StockReservationRequestedConsumer>>());
    
    var evt = new StockReservationRequestedEvent
    {
        OrderId = Guid.NewGuid(),
        Items = new[] { new StockReservationItem { ProductId = product.Id, Quantity = 5 } }
    };
    var context = new Mock<ConsumeContext<StockReservationRequestedEvent>>();
    context.Setup(x => x.Message).Returns(evt);
    context.Setup(x => x.Publish(It.IsAny<StockReservedEvent>(), It.IsAny<CancellationToken>()))
           .Returns(Task.CompletedTask);
    
    // Act
    await consumer.Consume(context.Object);
    
    // Assert
    context.Verify(x => x.Publish(It.Is<StockReservedEvent>(e => 
        e.OrderId == evt.OrderId && 
        e.Items.Single().Quantity == 5), 
        It.IsAny<CancellationToken>()), Times.Once);
}
```

### [HIGH]: Product CRUD Integration Tests  
**For**: ProductsController.cs:Update,Delete
**Type**: Integration
**Why**: Missing integration coverage for core product mutation operations
**Skeleton**:
```csharp
[Fact]
public async Task UpdateProduct_PublishesInvalidationEvent_WhenSuccessful()
{
    // Arrange
    var categoryId = await CreateCategoryAsync();
    var productId = await CreateProductAsync(categoryId);
    var harness = _factory.Services.GetRequiredService<ITestHarness>();
    
    // Act
    var updateResp = await _client.PutAsJsonAsync($"/api/v1/products/{productId}", new
    {
        name = "Updated Name",
        description = "Updated Desc", 
        unitPrice = 99.99m,
        categoryId = categoryId,
        isListed = true
    });
    
    // Assert
    updateResp.StatusCode.Should().Be(HttpStatusCode.OK);
    var published = await harness.Published.Any<ProductCacheInvalidatedEvent>();
    published.Should().BeTrue();
}
```

### [HIGH]: Domain Entity Unit Tests
**For**: Category.cs, ProductReview.cs  
**Type**: Unit
**Why**: Domain entities contain business logic that needs isolated testing
**Skeleton**:
```csharp
[Fact]
public void Category_Rename_UpdatesNameAndModifiedDate()
{
    // Arrange
    var category = Category.Create("Original", "Desc");
    var before = category.LastModifiedDate;
    
    // Act
    category.Rename("New Name");
    
    // Assert
    category.Name.Should().Be("New Name");
    category.LastModifiedDate.Should().BeAfter(before);
}

[Fact]
public void ProductReview_Approve_SetsIsApprovedTrue()
{
    // Arrange  
    var review = ProductReview.Create(Guid.NewGuid(), "user1", 5, "Great!", "John", "Title");
    
    // Act
    review.Approve();
    
    // Assert
    review.IsApproved.Should().BeTrue();
}
```

### [HIGH]: ProductCacheReader Unit Tests
**For**: ProductCacheReader.cs:GetAsync,InvalidateAsync
**Type**: Unit
**Why**: Critical caching component with complex hit/miss logic and no test coverage  
**Skeleton**:
```csharp
[Fact] 
public async Task GetAsync_ReturnsCachedResult_OnSecondCall()
{
    // Arrange
    var cache = new Mock<HybridCache>();
    var repository = new Mock<IProductRepository>();
    var product = Product.Create("Test", "Desc", 1000L, "USD", Guid.NewGuid());
    
    repository.Setup(x => x.GetByIdAsync(It.IsAny<Guid>(), It.IsAny<CancellationToken>()))
              .ReturnsAsync(product);
              
    cache.Setup(x => x.GetOrCreateAsync<ProductDto?>(
        It.IsAny<string>(), 
        It.IsAny<Func<CancellationToken, ValueTask<ProductDto?>>>(),
        It.IsAny<HybridCacheEntryOptions>(),
        It.IsAny<IEnumerable<string>>(),
        It.IsAny<CancellationToken>()))
         .Returns((string key, Func<CancellationToken, ValueTask<ProductDto?>> factory, HybridCacheEntryOptions opts, IEnumerable<string> tags, CancellationToken ct) => factory(ct));
    
    var reader = new ProductCacheReader(cache.Object, repository.Object, Mock.Of<ILogger<ProductCacheReader>>());
    
    // Act
    var result1 = await reader.GetAsync(product.Id, CancellationToken.None);
    var result2 = await reader.GetAsync(product.Id, CancellationToken.None);
    
    // Assert
    repository.Verify(x => x.GetByIdAsync(product.Id, It.IsAny<CancellationToken>()), Times.Once);
    result1.Source.Should().Be("database");
}
```

### [MEDIUM]: Demo Controller Integration Tests
**For**: DemoConcurrencyController.cs, DemoTestController.cs
**Type**: Integration  
**Why**: Demo endpoints need testing to ensure portfolio site integration works
**Skeleton**:
```csharp
[Fact]
public async Task ConcurrencyDemo_Update_ReturnsOptimisticLockError_OnStaleVersion()
{
    // Arrange
    var categoryId = await CreateCategoryAsync();
    var productId = await CreateProductAsync(categoryId, initialStock: 10);
    
    var getResp = await _client.GetAsync($"/demo/inventory/{productId}");
    var inventory = await getResp.Content.ReadFromJsonAsync<InventorySnapshot>();
    
    // Simulate concurrent update by directly updating via repository
    await SimulateConcurrentUpdate(productId);
    
    // Act - try to update with stale version  
    var updateResp = await _client.PutAsJsonAsync($"/demo/inventory/{productId}", 
        new { quantity = 5 },
        new { Headers = new { "If-Match" = inventory.Version } });
    
    // Assert
    updateResp.StatusCode.Should().Be(HttpStatusCode.PreconditionFailed);
}
```

## Test Optimization Violations

### [MEDIUM]: Missing Repository Unit Tests
**File**: All repository classes
**Violation**: No isolated unit tests for repository logic
**Fix**: Add unit tests using in-memory DbContext or mocked EF context

### [LOW]: Integration Test Organization  
**File**: Various integration test files
**Violation**: Some integration tests could be better organized by feature area
**Fix**: Consider grouping related endpoint tests into feature-specific test classes

## E2E Coverage Gaps

### [MISSING] Product Review Lifecycle Flow
**Services involved**: BffWeb → Identity → Catalog
**Description**: End-to-end user registration → product review creation → admin approval → public visibility

### [MISSING] Concurrency Demo Flow  
**Services involved**: BffWeb → Catalog  
**Description**: Portfolio site's optimistic concurrency demo using DemoConcurrencyController

### [MISSING] Cache Invalidation Demo Flow
**Services involved**: BffWeb → Catalog
**Description**: Product update → cache invalidation → HybridCache demonstration

## Summary

- **Total public methods**: ~45, **covered**: ~25 (56%)
- **API endpoints tested**: 8/18 (44%)  
- **Consumer handlers tested**: 2/3 (67%)
- **E2E flows tested**: 3/6 (50%)
- **Test optimization violations**: 2
- **Critical gaps**: 8

**Key Recommendations**:
1. **Immediate Priority**: Add ProductReviewsController integration tests (security-critical)
2. **High Priority**: Add StockReservationRequestedConsumer unit tests (saga choreography reliability) 
3. **Medium Priority**: Complete ProductsController integration coverage (core CRUD operations)
4. **Ongoing**: Add domain entity unit tests for business logic validation

The most critical gap is the **Product Review system** which has complex authorization logic but minimal test coverage, followed by **consumer reliability** which is essential for the saga choreography patterns.
