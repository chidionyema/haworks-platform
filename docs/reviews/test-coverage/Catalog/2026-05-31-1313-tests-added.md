I need to get approval for this workflow. Here's what it will do:

## Workflow Plan: Catalog Test Coverage

**Phases:**
1. **Critical Unit Tests** - StockReservationRequestedConsumer (162 lines, zero coverage) + domain entities
2. **Handler Unit Tests** - Command handlers and infrastructure components  
3. **Integration Tests** - API controller tests for ProductReviews, Products CRUD, Demo controllers
4. **Verification** - Build + test all new code

**Expected Output:**
- ~15-20 new test files
- ~50-80 test methods total
- Addresses 38 confirmed gaps from your audit
- Critical consumer tests (highest priority)
- Full ProductReview API coverage 
- Missing Product CRUD operations

**Safety:**
- Only creates test files, never modifies source code
- Follows mandatory test tier rules (unit vs integration)
- Uses required patterns (SharedTestPostgres, MigrateAsync, etc.)
- Includes build verification to catch compilation issues

Should I proceed with this workflow to systematically create all the missing tests?
