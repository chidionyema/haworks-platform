=== Test Coverage Audit: Location (2026-06-01T12:26:42Z) ===
Source files: 20
Unit test files: 3
Integration test files: 2
E2E test files: 14

# Location Service Test Coverage Audit

## Coverage Map

### API Endpoints
- [COVERED] POST `/api/v1/addresses` — tested by LocationFlowsTests.CreateAddress_ShouldReturnIdAndPersistData
- [COVERED] GET `/api/v1/addresses/nearby` — tested by LocationFlowsTests.GetNearby_ShouldReturnCorrectLocations
- [MISSING] gRPC LocationHydration.GetAddresses — no test found, PRIORITY: HIGH

### Command Handlers  
- [PARTIAL] CreateAddressCommandHandler — happy path only, missing: geocoding failures, duplicate address handling, validation edge cases
- [COVERED] CreateAddressCommandValidator — tested by CreateAddressCommandValidatorTests

### Query Handlers
- [MISSING] GetNearbyAddressesQueryHandler — no unit test found, PRIORITY: HIGH
- [COVERED] GetNearbyAddressesQueryValidator — tested by GetNearbyAddressesQueryValidatorTests

### Domain Logic
- [MISSING] Address.Create method — no unit tests, PRIORITY: MEDIUM
- [MISSING] Address domain invariants — no tests for business rules

### Infrastructure Services
- [MISSING] GeohashService.Encode — no unit tests, PRIORITY: MEDIUM  
- [MISSING] NominatimGeocodingService — no unit tests, PRIORITY: HIGH
- [MISSING] LocationDbContext custom behavior — no tests for spatial queries, schema creation

### Integration Patterns
- [MISSING] MassTransit Outbox integration — no tests verifying event publishing
- [MISSING] PostGIS spatial queries — no tests for geography operations
- [MISSING] Memory cache behavior — cache hit/miss scenarios untested

## Missing Tests (ordered by priority)

### [HIGH]: gRPC LocationHydrationService Tests
- **For**: LocationHydrationService.cs:GetAddresses
- **Type**: Integration  
- **Why**: External API contract with no validation - could break Search service hydration
- **Skeleton**:
```csharp
[Fact]
public async Task GetAddresses_WithValidLocationIds_ShouldReturnAddressDetails()
{
    // Arrange
    var service = new LocationHydrationService(_dbContext, _logger);
    var address = await SeedAddressAsync();
    var request = new AddressRequest();
    request.LocationIds.Add(address.Id.ToString());

    // Act
    var response = await service.GetAddresses(request, TestServerCallContext.Create());

    // Assert
    response.Locations.Should().HaveCount(1);
    response.Locations[0].Id.Should().Be(address.Id.ToString());
}

[Fact] 
public async Task GetAddresses_WithTooManyIds_ShouldThrowInvalidArgument()
{
    // Arrange
    var request = new AddressRequest();
    for(int i = 0; i < 1001; i++) request.LocationIds.Add(Guid.NewGuid().ToString());

    // Act & Assert
    await Assert.ThrowsAsync<RpcException>(async () => 
        await _service.GetAddresses(request, TestServerCallContext.Create()));
}
```

### [HIGH]: NominatimGeocodingService Unit Tests
- **For**: NominatimGeocodingService.cs:GeocodeAsync
- **Type**: Unit
- **Why**: External dependency failure could break address creation; no retry/timeout validation
- **Skeleton**:
```csharp
[Fact]
public async Task GeocodeAsync_WithValidAddress_ShouldReturnCoordinates()
{
    // Arrange
    var mockHandler = new Mock<HttpMessageHandler>();
    mockHandler.SetupRequest(HttpMethod.Get, It.IsAny<string>())
        .ReturnsResponse(HttpStatusCode.OK, """[{"lat":"51.5074","lon":"-0.1278"}]""");
    var httpClient = new HttpClient(mockHandler.Object) { BaseAddress = new Uri("https://test/") };
    var service = new NominatimGeocodingService(httpClient, _logger);

    // Act  
    var result = await service.GeocodeAsync("10 Downing Street, London");

    // Assert
    result.Should().NotBeNull();
    result.Value.Latitude.Should().BeApproximately(51.5074, 0.001);
}

[Fact]
public async Task GeocodeAsync_WithHttpTimeout_ShouldReturnNull()
{
    // Arrange - setup timeout scenario
    // Act & Assert - should handle gracefully
}
```

### [HIGH]: GetNearbyAddressesQueryHandler Unit Tests  
- **For**: GetNearbyAddressesQueryHandler.cs:Handle
- **Type**: Unit
- **Why**: Spatial query logic and caching behavior completely untested
- **Skeleton**:
```csharp
[Fact]
public async Task Handle_WithCachedResults_ShouldReturnFromCache()
{
    // Arrange
    var query = new GetNearbyAddressesQuery(51.5, -0.1, 1000);
    var cached = new List<NearbyAddressDto> { /* test data */ };
    _cache.Setup(x => x.TryGetValue(It.IsAny<string>(), out cached)).Returns(true);

    // Act
    var result = await _handler.Handle(query, CancellationToken.None);

    // Assert
    result.IsSuccess.Should().BeTrue();
    _dbContext.Verify(x => x.Addresses, Times.Never); // Should not hit DB
}
```

### [MEDIUM]: CreateAddressCommand Error Path Tests
- **For**: CreateAddressCommandHandler.cs:Handle  
- **Type**: Unit
- **Why**: Geocoding failures and duplicate handling not tested
- **Skeleton**:
```csharp
[Fact] 
public async Task Handle_WhenGeocodingFailsCompletely_ShouldReturnFailure()
{
    // Arrange
    var command = new CreateAddressCommand { /* no coordinates */ };
    _geocodingMock.Setup(x => x.GeocodeAsync(It.IsAny<string>(), It.IsAny<CancellationToken>()))
        .ReturnsAsync((ValueTuple<double, double>?)null);

    // Act
    var result = await _handler.Handle(command, CancellationToken.None);

    // Assert
    result.IsSuccess.Should().BeFalse();
    result.Error.Code.Should().Be("Address.GeocodingFailed");
}
```

### [MEDIUM]: GeohashService Unit Tests
- **For**: GeohashService.cs:Encode
- **Type**: Unit  
- **Why**: No validation of geohash algorithm correctness - could affect spatial indexing
- **Skeleton**:
```csharp
[Theory]
[InlineData(51.5074, -0.1278, 12, "gcpvj0du6ux0")] // London
[InlineData(40.7128, -74.0060, 12, "dr5regw3kgs4")] // NYC
public void Encode_WithKnownCoordinates_ShouldReturnExpectedGeohash(double lat, double lon, int precision, string expected)
{
    // Act
    var result = _service.Encode(lat, lon, precision);
    
    // Assert  
    result.Should().Be(expected);
    result.Should().HaveLength(precision);
}
```

## Test Optimization Violations

### [HIGH]: Integration Test Factory Issues  
- **File**: tests/Location/Location.Integration/LocationWebAppFactory.cs:51
- **Violation**: Mocks core services instead of using real implementations
- **Fix**: Remove IGeocodingService and IPublishEndpoint mocks; test real geocoding with test doubles

### [MEDIUM]: Missing Real MassTransit Testing
- **File**: tests/Location/Location.Integration/LocationWebAppFactory.cs:56  
- **Violation**: Mocked IPublishEndpoint breaks outbox testing
- **Fix**: Use real MassTransit with SharedTestRabbitMq and verify outbox messages

### [MEDIUM]: PostGIS Container Usage
- **File**: tests/Location/Location.Integration/LocationWebAppFactory.cs:15
- **Violation**: ✅ Correctly uses SharedTestPostGis (not SharedTestPostgres)
- **Status**: COMPLIANT - properly configured for geospatial testing

## E2E Coverage Gaps

### [MISSING] gRPC Cross-Service Flow — Location ↔ Search service hydration
- Services: location-svc (gRPC) → search-svc (HTTP client)
- **E2E Gap**: No test verifying Search service can hydrate location data via gRPC

### [MISSING] Geocoding Integration Flow — External API reliability  
- Services: location-svc → Nominatim API → PostGIS storage
- **E2E Gap**: No test with real external geocoding service (rate limits, failures)

### [MISSING] Event Publishing Flow — Address creation triggers search indexing
- Services: location-svc → MassTransit → search-svc  
- **E2E Gap**: Verify LocationUpdated events actually reach search consumers

## Summary

- **Total public methods**: 12, **covered**: 6 (50%)
- **API endpoints tested**: 2/3 (missing gRPC)  
- **Command handlers tested**: 1/1 (partial coverage)
- **Query handlers tested**: 0/1
- **Infrastructure services tested**: 0/3
- **Test optimization violations**: 2
- **Critical gaps**: 6

### Key Risks
1. **gRPC service untested** — Search hydration could fail silently  
2. **Spatial query logic untested** — PostGIS distance calculations unverified
3. **External geocoding failure modes untested** — Could break address creation
4. **Cache behavior untested** — Performance and correctness issues possible
5. **Event publishing untested** — Cross-service consistency at risk

### Recommendations
1. Add gRPC integration tests using TestServer and real grpc-dotnet client
2. Create comprehensive geocoding service tests with failure scenarios  
3. Test PostGIS spatial queries with real geography data
4. Verify MassTransit outbox pattern with real message transport
5. Add E2E tests covering the complete address creation → search indexing flow
