=== Test Coverage Audit: Location (2026-05-30T21:21:24Z) ===
Source files: 20
Unit test files: 3
Integration test files: 2
E2E test files: 14

# Location Service Test Coverage Audit

## Coverage Map

### API Layer
- [PARTIAL] `AddressesController.cs` — covered by integration tests only, missing unit test coverage for controller logic
- [MISSING] `LocationHydrationService.cs` — no tests found, PRIORITY: HIGH
- [COVERED] `Program.cs` — tested indirectly via integration tests

### Application Layer  
- [COVERED] `CreateAddressCommand.cs` + Handler — tested by CreateAddressCommandHandlerTests.cs
- [COVERED] `CreateAddressCommandValidator.cs` — tested by CreateAddressCommandValidatorTests.cs
- [PARTIAL] `GetNearbyAddressesQuery.cs` — validator tested, missing handler unit tests
- [COVERED] `GetNearbyAddressesQueryValidator.cs` — tested by GetNearbyAddressesQueryValidatorTests.cs
- [MISSING] `GetNearbyAddressesQueryHandler.cs` — no unit tests, PRIORITY: HIGH
- [MISSING] Application interfaces — no contract verification tests, PRIORITY: MEDIUM

### Domain Layer
- [MISSING] `Address.cs` entity — no unit tests for Create method, PRIORITY: HIGH

### Infrastructure Layer
- [MISSING] `GeohashService.cs` — no unit tests, PRIORITY: HIGH  
- [MISSING] `NominatimGeocodingService.cs` — no unit tests, PRIORITY: HIGH
- [PARTIAL] `LocationDbContext.cs` — integration tested, missing unit tests for audit stamping logic
- [MISSING] Database constraints and indexes — no integration tests, PRIORITY: MEDIUM

## Missing Tests (ordered by priority)

### [HIGH]: GetNearbyAddressesQueryHandler unit tests
- **For**: GetNearbyAddressesQueryHandler.cs:Handle method
- **Type**: Unit
- **Why**: Core business logic for geospatial queries with caching - no unit test coverage
- **Skeleton**:
```csharp
public class GetNearbyAddressesQueryHandlerTests
{
    [Fact]
    public async Task Handle_WithCachedResults_ShouldReturnFromCache()
    {
        // Arrange
        var query = new GetNearbyAddressesQuery(51.5, -0.1, 5000, 20);
        var cachedResults = new List<NearbyAddressDto> { /* test data */ };
        var cache = new Mock<IMemoryCache>();
        cache.Setup(x => x.TryGetValue(It.IsAny<string>(), out cachedResults)).Returns(true);
        
        // Act & Assert
        // Test cache hit path
    }
    
    [Fact] 
    public async Task Handle_WithNoCacheHit_ShouldQueryDatabaseAndCache()
    {
        // Test cache miss path with database query
    }
}
```

### [HIGH]: LocationHydrationService gRPC tests
- **For**: LocationHydrationService.cs:GetAddresses
- **Type**: Unit + Integration
- **Why**: Critical gRPC endpoint for cross-service communication - completely untested
- **Skeleton**:
```csharp
public class LocationHydrationServiceTests
{
    [Fact]
    public async Task GetAddresses_WithValidIds_ShouldReturnAddresses()
    {
        // Arrange
        var request = new AddressRequest();
        request.LocationIds.AddRange(new[] { Guid.NewGuid().ToString() });
        
        // Act & Assert
        // Test successful hydration
    }
    
    [Fact]
    public async Task GetAddresses_WithTooManyIds_ShouldThrowRpcException()
    {
        // Test >1000 ID limit validation
    }
}
```

### [HIGH]: GeohashService encoding tests  
- **For**: GeohashService.cs:Encode method
- **Type**: Unit
- **Why**: Geospatial indexing logic with no verification of algorithm correctness
- **Skeleton**:
```csharp
public class GeohashServiceTests
{
    [Theory]
    [InlineData(51.5074, -0.1278, 12, "gcpuvpk44kpr")]
    [InlineData(0, 0, 5, "s0000")]
    public void Encode_WithKnownCoordinates_ShouldReturnExpectedGeohash(
        double lat, double lon, int precision, string expected)
    {
        // Test against known geohash values
    }
}
```

### [HIGH]: NominatimGeocodingService HTTP client tests
- **For**: NominatimGeocodingService.cs:GeocodeAsync
- **Type**: Unit
- **Why**: External service integration with no error handling tests
- **Skeleton**:
```csharp
public class NominatimGeocodingServiceTests
{
    [Fact]
    public async Task GeocodeAsync_WithValidResponse_ShouldReturnCoordinates()
    {
        // Test successful geocoding with HttpClientFactory mock
    }
    
    [Fact] 
    public async Task GeocodeAsync_WithHttpError_ShouldReturnNull()
    {
        // Test HTTP error handling
    }
}
```

### [HIGH]: Address domain entity tests
- **For**: Address.cs:Create method
- **Type**: Unit  
- **Why**: Domain logic creation method with no business rule verification
- **Skeleton**:
```csharp
public class AddressTests
{
    [Fact]
    public void Create_WithValidData_ShouldCreateAddress()
    {
        // Arrange
        var coordinates = new Point(-0.1278, 51.5074) { SRID = 4326 };
        
        // Act
        var address = Address.Create("123 Main St", "London", "SW1A 1AA", "UK", coordinates, "gcpvj0d9");
        
        // Assert
        address.Should().NotBeNull();
        address.Street.Should().Be("123 Main St");
        address.Coordinates.Should().Be(coordinates);
    }
}
```

### [MEDIUM]: Unique constraint violation handling
- **For**: CreateAddressCommand duplicate address handling
- **Type**: Integration
- **Why**: Database constraint enforcement not verified
- **Skeleton**:
```csharp
[Fact]
public async Task CreateAddress_WithDuplicateAddress_ShouldReturnValidationError()
{
    // Create same address twice, verify constraint violation handling
}
```

### [MEDIUM]: Memory cache behavior verification
- **For**: GetNearbyAddressesQueryHandler caching logic
- **Type**: Integration  
- **Why**: Cache TTL and eviction behavior not tested
- **Skeleton**:
```csharp
[Fact]
public async Task GetNearby_RepeatedCalls_ShouldUseCacheWithCorrectTtl()
{
    // Test cache expiration after 5 minutes
}
```

### [MEDIUM]: Idempotency key validation
- **For**: CreateAddressCommand idempotency behavior
- **Type**: Integration
- **Why**: At-most-once execution guarantee not verified
- **Skeleton**:
```csharp
[Fact]
public async Task CreateAddress_WithSameIdempotencyKey_ShouldReturnSameResult()
{
    // Test idempotent execution via IdempotencyJournal
}
```

## Test Optimization Violations

### [MEDIUM]: Integration test mocking external services
- **File**: LocationWebAppFactory.cs:ConfigureTestServices
- **Violation**: Mocks `IGeocodingService` instead of using real HTTP client with test doubles
- **Fix**: Remove geocoding mock, configure HttpClient to use test server or WireMock for realistic integration testing

### [LOW]: Unit test using mocked DbContext
- **File**: CreateAddressCommandHandlerTests.cs:CreateMockDbSet  
- **Violation**: Manual DbSet mocking instead of in-memory provider for cleaner unit tests
- **Fix**: Use EF Core InMemory provider: `options.UseInMemoryDatabase("test-db")`

## E2E Coverage Gaps

- [MISSING] Cross-service gRPC hydration — other services calling LocationHydrationService
- [MISSING] Event publishing verification — LocationUpdated events reaching message bus  
- [MISSING] Geospatial search integration — location data searchable via Search service
- [MISSING] PostGIS extension functionality — geospatial indexing performance under load

## Summary

- **Total public methods**: 12, covered: 4 (33%)
- **API endpoints tested**: 2/3 (missing gRPC hydration)  
- **Application handlers tested**: 1/2 (missing query handler)
- **Infrastructure services tested**: 0/2 (missing geocoding + geohash)
- **Test optimization violations**: 2
- **Critical gaps**: 5 (gRPC service, query handler, geocoding, geohash, domain entity)

The Location service has significant test coverage gaps, particularly around the gRPC hydration service and core infrastructure components. The geospatial functionality (geohash encoding, PostGIS queries) lacks verification, creating risk for location-based features.
