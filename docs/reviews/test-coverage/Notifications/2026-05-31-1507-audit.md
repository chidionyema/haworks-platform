=== Test Coverage Audit: Notifications (2026-05-31T15:07:53Z) ===
Source files: 90
Unit test files: 18
Integration test files: 4
E2E test files: 14

## Notifications Service Test Coverage Audit

### Coverage Map

#### API Controllers
- [PARTIAL] NotificationsController.Send — happy path tested, missing preference/suppression integration tests
- [PARTIAL] NotificationsController.Get — unit tested only, missing IDOR integration tests
- [PARTIAL] WebhooksController.Ses — basic test only, missing SNS signature validation
- [PARTIAL] WebhooksController.SendGrid — basic test only, missing ECDSA verification
- [PARTIAL] WebhooksController.Twilio — basic test only, missing full webhook flow

#### MediatR Handlers  
- [COVERED] SendNotificationCommandHandler — tested by SendNotificationCommandHandlerTests.HandleHappyPath
- [MISSING] GetNotificationQueryHandler — no dedicated unit tests, PRIORITY: HIGH
- [PARTIAL] UpdateNotificationStatusFromWebhookCommandHandler — happy path only, missing: error scenarios

#### Domain Entities
- [COVERED] Notification — tested by NotificationTests.HappyPath_CreatedRenderingQueuedSentDelivered_AdvancesStatus
- [MISSING] RateLimitBucket — no test found, PRIORITY: MEDIUM
- [PARTIAL] NotificationTemplate — factory only, missing: validation logic
- [PARTIAL] NotificationPreference — factory only, missing: quiet hours edge cases
- [PARTIAL] Suppression — factory only, missing: validation edge cases

#### Message Consumers
- [COVERED] NotificationRequestConsumer — tested by NotificationRequestConsumerTests.Consume_HappyPath
- [MISSING] RefundEmailConsumer — no test found, PRIORITY: HIGH
- [MISSING] SecretExpiryWarningConsumer — no test found, PRIORITY: HIGH  
- [MISSING] NotificationWebhookValidatedConsumer — no test found, PRIORITY: HIGH

#### Services
- [COVERED] PreferencesService — tested by PreferenceTests.IsAllowedAsync_HappyPath
- [COVERED] SuppressionService — tested by SuppressionTests.IsSuppressedAsync_HashHit_BlocksRecipient
- [COVERED] IdempotencyKeyGenerator — tested by IdempotencyTests.Generate_SameInputs_ProducesSameKey
- [COVERED] TemplateSelector — tested by TemplateTests.SelectAsync_ExactLocaleAvailable_ReturnsExactMatch
- [COVERED] ScribanTemplateRenderer — tested by TemplateTests.Render_ValidVariables_SubstitutesIntoSubjectAndBody

#### Channel Gateways & Providers
- [COVERED] EmailChannelGateway — tested by EmailChannelGatewayTests.SendAsync_PrimaryRetryable_SecondarySuccess
- [COVERED] SesEmailProvider — tested by SesEmailProviderTests.SendAsync_OnHttpOk_ReturnsSuccessWithMessageId
- [COVERED] SendGridEmailProvider — tested by SendGridEmailProviderTests.SendAsync_On2xx_ReturnsSuccessWithMessageId
- [COVERED] TwilioSmsProvider — tested by TwilioSmsProviderTests.SendAsync_OnSuccess_ReturnsSuccessWithSid
- [MISSING] FcmPushProvider — no test found, PRIORITY: MEDIUM

### Missing Tests (ordered by priority)

#### [HIGH]: GetNotificationQueryHandler unit tests
- **For**: GetNotificationQueryHandler.Handle  
- **Type**: Unit
- **Why**: Core query handler has no dedicated tests; IDOR protection logic untested
- **Skeleton**:
```csharp
[Fact]
public async Task Handle_ValidId_ReturnsNotificationDto()
{
    // Arrange
    var notification = CreateTestNotification();
    _repository.Setup(r => r.GetByIdAsync(notification.Id, It.IsAny<CancellationToken>()))
              .ReturnsAsync(notification);
    
    // Act
    var result = await _handler.Handle(new GetNotificationQuery(notification.Id), CancellationToken.None);
    
    // Assert
    result.IsSuccess.Should().BeTrue();
    result.Value.Id.Should().Be(notification.Id);
}
```

#### [HIGH]: RefundEmailConsumer tests
- **For**: RefundEmailConsumer.Consume
- **Type**: Unit | Integration  
- **Why**: No tests for critical payment event consumer; email notifications for refunds untested
- **Skeleton**:
```csharp
[Fact]
public async Task Consume_RefundCompletedEvent_SendsNotificationCommand()
{
    // Arrange
    var evt = new RefundCompletedEvent 
    { 
        RefundId = Guid.NewGuid(), 
        CustomerEmail = "user@test.com",
        AmountCents = 5000,
        Currency = "USD"
    };
    var context = Mock.Of<ConsumeContext<RefundCompletedEvent>>(c => c.Message == evt);
    
    // Act
    await _consumer.Consume(context);
    
    // Assert
    _mediator.Verify(m => m.Send(It.Is<SendNotificationCommand>(cmd => 
        cmd.Recipient == evt.CustomerEmail && 
        cmd.TemplateId == "refund-completed"), It.IsAny<CancellationToken>()), Times.Once);
}
```

#### [HIGH]: SecretExpiryWarningConsumer tests
- **For**: SecretExpiryWarningConsumer.Consume
- **Type**: Unit | Integration
- **Why**: Security alert consumer has no tests; critical system notifications could fail silently  
- **Skeleton**:
```csharp
[Fact]
public async Task Consume_SecretExpiryWarningEvent_LogsCriticalAlert()
{
    // Arrange  
    var evt = new SecretExpiryWarningEvent 
    { 
        SecretPath = "vault/path/secret", 
        AgePercent = 0.95,
        LastRotatedAt = DateTime.UtcNow.AddDays(-30)
    };
    var context = Mock.Of<ConsumeContext<SecretExpiryWarningEvent>>(c => c.Message == evt);
    
    // Act
    await _consumer.Consume(context);
    
    // Assert
    _logger.Verify(l => l.Log(LogLevel.Critical, It.IsAny<EventId>(), 
        It.Is<It.IsAnyType>((v, t) => v.ToString()!.Contains("ROTATION ALERT")), 
        null, It.IsAny<Func<It.IsAnyType, Exception?, string>>()), Times.Once);
}
```

#### [HIGH]: NotificationWebhookValidatedConsumer tests  
- **For**: NotificationWebhookValidatedConsumer.Consume
- **Type**: Unit | Integration
- **Why**: Webhook processing pipeline has no end-to-end tests; webhook events could be lost
- **Skeleton**:
```csharp
[Fact]
public async Task Consume_ValidatedWebhookEvent_SendsUpdateCommand()
{
    // Arrange
    var evt = new NotificationWebhookValidatedEvent
    {
        Provider = "SES",
        ProviderEventId = "msg-123", 
        EventType = "Delivery",
        RawPayload = "{}"
    };
    var context = Mock.Of<ConsumeContext<NotificationWebhookValidatedEvent>>(c => c.Message == evt);
    
    // Act  
    await _consumer.Consume(context);
    
    // Assert
    _mediator.Verify(m => m.Send(It.Is<UpdateNotificationStatusFromWebhookCommand>(cmd =>
        cmd.Provider == "SES" && cmd.EventType == "Delivery"), It.IsAny<CancellationToken>()), Times.Once);
}
```

#### [MEDIUM]: FcmPushProvider tests
- **For**: FcmPushProvider.SendAsync
- **Type**: Unit
- **Why**: Push notification provider has no tests; Firebase integration could fail silently
- **Skeleton**:
```csharp
[Fact]  
public async Task SendAsync_ValidToken_ReturnsSuccessWithMessageId()
{
    // Arrange
    _firebaseMessaging.Setup(m => m.SendAsync(It.IsAny<Message>(), It.IsAny<CancellationToken>()))
                     .ReturnsAsync("firebase-msg-123");
    
    // Act
    var result = await _provider.SendAsync("device-token", "Subject", "Body", CancellationToken.None);
    
    // Assert
    result.IsSuccess.Should().BeTrue();
    result.ProviderMessageId.Should().Be("firebase-msg-123");
}
```

#### [MEDIUM]: RateLimitBucket entity tests
- **For**: RateLimitBucket.Create
- **Type**: Unit
- **Why**: Rate limiting entity has no validation tests; bucket creation logic untested
- **Skeleton**:
```csharp
[Fact]
public void Create_ValidParameters_ReturnsBucket()
{
    // Arrange
    var bucketKey = "user123|Email|category";
    var windowStart = DateTime.UtcNow.Date;
    
    // Act
    var bucket = RateLimitBucket.Create(bucketKey, windowStart, 5);
    
    // Assert
    bucket.BucketKey.Should().Be(bucketKey);
    bucket.WindowStart.Should().Be(windowStart);
    bucket.Count.Should().Be(5);
}
```

### Test Optimization Violations

#### [WARNING]: Integration tests skipped due to MassTransit double registration
- **File**: tests/Notifications/Notifications.Integration/NotificationsApiTests.cs:19
- **Violation**: Tests marked with `[Fact(Skip=...)]` due to infrastructure issue
- **Fix**: Merge duplicate `AddMassTransit()` calls in Application and Infrastructure layers into single registration

#### [INFO]: Proper test infrastructure usage detected
- **File**: tests/Notifications/Notifications.Integration/NotificationsWebAppFactory.cs:41
- **Verification**: Correctly uses `SharedTestPostgres.CreateDatabaseAsync("notifications")`
- **File**: tests/Notifications/Notifications.Integration/NotificationsWebAppFactory.cs:156  
- **Verification**: Correctly uses `MigrateAsync()` not `EnsureCreatedAsync()`

### E2E Coverage Gaps

- [MISSING] Notification sending flow — end-to-end notification dispatch not covered
- [MISSING] Email delivery verification — no E2E tests verify actual email dispatch  
- [MISSING] Webhook delivery flow — no E2E tests for webhook→status update chain
- [MISSING] Provider failover testing — no E2E tests for multi-provider scenarios

E2E tests MUST be in tests/E2E/, gated by E2E_ENABLED=1

### Summary

- **Total public methods**: 47, **covered**: 31 (66%)
- **API endpoints tested**: 5/5 (basic), missing full integration coverage
- **Consumer handlers tested**: 1/4 (25%)  
- **E2E flows tested**: 0/4 (0%)
- **Test optimization violations**: 1 (MassTransit double registration blocking integration tests)
- **Critical gaps**: 4 (missing consumers, query handler)

**Immediate Actions Needed:**
1. Fix MassTransit double registration to enable integration tests
2. Add unit tests for missing consumers (RefundEmailConsumer, SecretExpiryWarningConsumer, NotificationWebhookValidatedConsumer)  
3. Add dedicated tests for GetNotificationQueryHandler
4. Add integration tests for webhook endpoints with signature validation
5. Add E2E tests for core notification workflows
