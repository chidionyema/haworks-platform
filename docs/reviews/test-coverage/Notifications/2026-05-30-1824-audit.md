=== Test Coverage Audit: Notifications (2026-05-30T18:24:29Z) ===
Source files: 90
Unit test files: 18
Integration test files: 4
E2E test files: 14

# Test Coverage Audit: src/Notifications

## Coverage Map

### API Controllers
- [COVERED] NotificationsController — tested by NotificationsControllerTests
- [COVERED] WebhooksController — tested by WebhooksControllerTests  
- [MISSING] Program.cs — no tests (typical for startup configuration)

### Application - Commands & Handlers
- [COVERED] SendNotificationCommand — tested by SendNotificationCommandHandlerTests
- [PARTIAL] UpdateNotificationStatusFromWebhookCommand — happy path only, missing webhook signature validation edge cases
- [COVERED] SendNotificationCommandValidator — tested by SendNotificationCommandValidatorTests
- [MISSING] UpdateNotificationStatusFromWebhookCommandValidator — no tests found

### Application - Queries
- [PARTIAL] GetNotificationQuery — validator tested, handler missing tests
- [MISSING] GetNotificationQueryValidator — no comprehensive validation tests

### Application - Consumers
- [COVERED] NotificationRequestConsumer — tested by NotificationRequestConsumerTests
- [MISSING] RefundEmailConsumer — no tests found, PRIORITY: HIGH
- [MISSING] SecretExpiryWarningConsumer — no tests found, PRIORITY: MEDIUM
- [MISSING] NotificationWebhookValidatedConsumer — no tests found, PRIORITY: HIGH

### Application - Templates
- [COVERED] ScribanTemplateRenderer — tested by TemplateTests
- [COVERED] TemplateSelector — tested by TemplateTests
- [MISSING] TemplateRepository implementation — repository interface tested indirectly, PRIORITY: MEDIUM

### Application - Preferences
- [COVERED] PreferencesService — comprehensively tested by PreferenceTests
- [MISSING] PreferencesRepository implementation — no unit tests, PRIORITY: MEDIUM
- [COVERED] PreferenceFactory — indirectly tested through PreferenceTests

### Application - Suppression  
- [COVERED] SuppressionService — tested by SuppressionTests
- [MISSING] SuppressionRepository implementation — no unit tests, PRIORITY: MEDIUM
- [COVERED] SuppressionFactory — indirectly tested through SuppressionService tests

### Application - Idempotency
- [COVERED] IdempotencyKeyGenerator — tested by IdempotencyTests

### Domain - Entities
- [COVERED] Notification — comprehensively tested by NotificationTests
- [MISSING] NotificationPreference.Create factory — no tests found, PRIORITY: LOW
- [MISSING] NotificationTemplate.Create factory — no tests found, PRIORITY: LOW
- [MISSING] Suppression.Create factory — no tests found, PRIORITY: LOW
- [MISSING] RateLimitBucket.Create factory — no tests found, PRIORITY: LOW

### Domain - Events
- [MISSING] All domain events (NotificationBouncedEvent, NotificationCreatedEvent, etc.) — no validation tests, PRIORITY: LOW

### Infrastructure - Channel Gateways
- [COVERED] EmailChannelGateway — tested by EmailChannelGatewayTests
- [COVERED] SmsChannelGateway — tested by SmsChannelGatewayTests 
- [COVERED] PushChannelGateway — tested by PushChannelGatewayTests
- [PARTIAL] Circuit breaker behavior — not thoroughly tested across all gateways

### Infrastructure - Email Providers
- [COVERED] SesEmailProvider — tested by SesEmailProviderTests
- [COVERED] SendGridEmailProvider — tested by SendGridEmailProviderTests
- [MISSING] Provider configuration validation — edge cases not covered

### Infrastructure - SMS/Push Providers
- [COVERED] TwilioSmsProvider — tested by TwilioSmsProviderTests
- [PARTIAL] FcmPushProvider — basic option validation only, full provider behavior missing
- [MISSING] FCM provider error handling scenarios — no comprehensive tests

### Infrastructure - Persistence
- [MISSING] NotificationRepository — no unit tests, only integration coverage, PRIORITY: HIGH
- [MISSING] PreferencesRepository — no unit tests, PRIORITY: MEDIUM
- [MISSING] SuppressionRepository — no unit tests, PRIORITY: MEDIUM
- [MISSING] TemplateRepository — no unit tests, PRIORITY: MEDIUM
- [MISSING] NotificationsDbContext configuration — no tests

## Missing Tests (Priority Order)

### [HIGH]: Consumer Tests
**For**: RefundEmailConsumer  
**Type**: Unit  
**Why**: Critical business logic for refund notification flow  
**Skeleton**:
```csharp
[Fact]
public async Task Consume_RefundCompleted_SendsNotificationWithCorrectTemplate()
{
    // Arrange
    var mediator = new Mock<IMediator>();
    var consumer = new RefundEmailConsumer(mediator.Object, logger);
    var evt = new RefundCompletedEvent { RefundId = Guid.NewGuid(), CustomerEmail = "test@example.com", AmountCents = 5000, Currency = "USD" };
    var context = Mock.Of<ConsumeContext<RefundCompletedEvent>>(c => c.Message == evt);
    
    // Act
    await consumer.Consume(context);
    
    // Assert
    mediator.Verify(m => m.Send(It.Is<SendNotificationCommand>(cmd => 
        cmd.Recipient == "test@example.com" && 
        cmd.TemplateId == "refund-completed" &&
        cmd.Variables["Amount"].Equals(50.00m)), It.IsAny<CancellationToken>()), Times.Once);
}
```

### [HIGH]: Repository Unit Tests
**For**: NotificationRepository.cs  
**Type**: Unit  
**Why**: Core data access layer with complex query logic  
**Skeleton**:
```csharp
[Fact]
public async Task FindIdByIdempotencyKeyAsync_ExistingKey_ReturnsId()
{
    // Arrange
    var options = DbContextOptionsBuilder.UseInMemoryDatabase("test");
    using var context = new NotificationsDbContext(options, env, logger, currentUser);
    var notification = Notification.Create("test@example.com", NotificationChannel.Email, "template", "idem-key");
    context.Notifications.Add(notification);
    await context.SaveChangesAsync();
    var repository = new NotificationRepository(context);
    
    // Act
    var result = await repository.FindIdByIdempotencyKeyAsync("idem-key", CancellationToken.None);
    
    // Assert
    result.Should().Be(notification.Id);
}
```

### [HIGH]: Webhook Consumer Tests  
**For**: NotificationWebhookValidatedConsumer  
**Type**: Unit  
**Why**: Handles external webhook events, critical for provider status updates  
**Skeleton**:
```csharp
[Fact] 
public async Task Consume_ValidatedEvent_CallsUpdateCommand()
{
    // Arrange
    var mediator = new Mock<IMediator>();
    var consumer = new NotificationWebhookValidatedConsumer(mediator.Object, logger);
    var evt = new NotificationWebhookValidatedEvent { Provider = "SES", ProviderEventId = "msg-123", EventType = "Delivery" };
    var context = Mock.Of<ConsumeContext<NotificationWebhookValidatedEvent>>(c => c.Message == evt);
    
    // Act
    await consumer.Consume(context);
    
    // Assert  
    mediator.Verify(m => m.Send(It.Is<UpdateNotificationStatusFromWebhookCommand>(cmd =>
        cmd.Provider == "SES" && cmd.ProviderMessageId == "msg-123"), It.IsAny<CancellationToken>()), Times.Once);
}
```

### [MEDIUM]: Query Handler Tests
**For**: GetNotificationQuery handler  
**Type**: Unit  
**Why**: Read path validation and authorization logic  
**Skeleton**:
```csharp
[Fact]
public async Task Handle_ValidId_ReturnsNotificationDto()
{
    // Arrange  
    var repository = new Mock<INotificationRepository>();
    var notification = Notification.Create("test@example.com", NotificationChannel.Email, "template", "key");
    repository.Setup(r => r.GetByIdAsync(notification.Id, It.IsAny<CancellationToken>())).ReturnsAsync(notification);
    var handler = new GetNotificationQueryHandler(repository.Object, logger);
    var query = new GetNotificationQuery(notification.Id);
    
    // Act
    var result = await handler.Handle(query, CancellationToken.None);
    
    // Assert
    result.IsSuccess.Should().BeTrue();
    result.Value.Id.Should().Be(notification.Id);
}
```

### [MEDIUM]: FCM Provider Tests
**For**: FcmPushProvider.cs  
**Type**: Unit  
**Why**: Push notification provider with Firebase-specific error handling  
**Skeleton**:
```csharp
[Fact]
public async Task SendAsync_FirebaseMessagingException_InvalidArgument_ReturnsNonRetryable()
{
    // Arrange
    var firebaseMessaging = new Mock<FirebaseMessaging>();
    firebaseMessaging.Setup(f => f.SendAsync(It.IsAny<Message>(), It.IsAny<CancellationToken>()))
        .ThrowsAsync(new FirebaseMessagingException(MessagingErrorCode.InvalidArgument, "Invalid token"));
    var provider = new FcmPushProvider(firebaseMessaging.Object, logger);
    
    // Act
    var result = await provider.SendAsync("invalid-token", "subject", "body", CancellationToken.None);
    
    // Assert
    result.IsSuccess.Should().BeFalse();
    result.IsRetryable.Should().BeFalse();
}
```

## Test Optimization Violations

### [HIGH]: Raw Testcontainers Usage
**File**: tests/Notifications/Notifications.Integration/NotificationsWebAppFactory.cs  
**Violation**: Uses PostgreSql container builder instead of SharedTestPostgres  
**Fix**: Replace with `SharedTestPostgres.CreateDatabaseAsync("notifications")`

### [MEDIUM]: Missing MigrateAsync Usage  
**File**: tests/Notifications/Notifications.Integration/NotificationsWebAppFactory.cs:line 89  
**Violation**: Uses `EnsureSchemaAsync` which calls `EnsureCreatedAsync()` internally  
**Fix**: Explicitly call `MigrateAsync()` in test setup

### [MEDIUM]: Missing Test Authentication Setup
**File**: Integration test factories  
**Violation**: No `JwtTestDefaults.SetTestEnvironmentVariables()` call  
**Fix**: Add in `InitializeAsync()` method

### [LOW]: Test Naming Convention
**File**: Multiple test files  
**Violation**: Some tests don't follow `Method_Scenario_ExpectedResult` pattern  
**Fix**: Rename to consistent pattern

## E2E Coverage Gaps

### [MISSING] Complete notification flow
E2E tests MUST be in tests/E2E/, gated by E2E_ENABLED=1. Missing flows:
- Send notification → provider dispatch → webhook callback → status update
- Preference-blocked notification → suppression list update  
- Template rendering → provider failover → delivery confirmation
- Rate limiting → quiet hours → frequency cap enforcement

### [MISSING] Cross-service integration  
- BFF → notifications-svc → provider → webhook callback
- Audit trail for notification events
- MassTransit outbox pattern validation

## Summary
- Total public methods: 93, covered: ~60 (65%)
- API endpoints tested: 4/6 (webhooks partially covered)
- Consumer handlers tested: 1/4
- E2E flows tested: 0/4
- Test optimization violations: 4
- Critical gaps: 8 (High priority missing tests)

**Immediate Action Required**: Add missing consumer tests (RefundEmailConsumer, NotificationWebhookValidatedConsumer) and repository unit tests before production deployment.
