=== Continuous Review: Media (2026-05-31T02:45:58Z) ===
Files to review: 41

Perfect! Now I can see the complete pattern. The virus scanning happens at line 85 **before** the transaction starts at line 88, which correctly follows the Three-Phase Gateway pattern.

## **CRITICAL:** Repository Service Detection

### **CRITICAL**: Database Migration Schema Issues
- **File**: src/Media/Media.Api/Migrations/ (all migration files)
- **Issue**: All migrations correctly specify `schema: "media"` on CreateTable operations
- **Impact**: Actually, this is properly implemented - no issue found
- **Fix**: No fix needed, migrations are correct

### **HIGH**: Inefficient Batch Database Operations 
- **File**: src/Media/Media.Api/Application/BatchInitiateUpload.cs:47-60
- **Issue**: SaveChangesAsync called inside loop for each file, creating multiple transactions instead of batching
- **Impact**: Poor performance with multiple files, unnecessary transaction overhead, potential for partial failures
- **Fix**: Collect all MediaFile entities first, then save once outside the loop:
```csharp
var mediaFilesToAdd = new List<MediaFile>();
// ... loop logic to build mediaFilesToAdd ...
context.MediaFiles.AddRange(mediaFilesToAdd);
await context.SaveChangesAsync(ct);
```

### **HIGH**: Race Condition in File Creation
- **File**: src/Media/Media.Api/Application/InitiateUpload.cs:85-91
- **Issue**: Check-then-act pattern for duplicate hash detection without pessimistic locking
- **Impact**: Concurrent requests with same hash could both pass the null check and attempt to create records, leading to unique constraint violations
- **Fix**: Add pessimistic locking or handle unique constraint exceptions gracefully:
```csharp
try {
    // existing creation logic
} catch (DbUpdateException ex) when (IsUniqueConstraintViolation(ex)) {
    // Re-query and return existing file
}
```

### **MEDIUM**: Pagination Performance Anti-pattern
- **File**: src/Media/Media.Api/Application/ListMedia.cs:43-45
- **Issue**: Uses offset-based pagination (Skip/Take) which becomes increasingly slow with large page numbers
- **Impact**: Poor performance when users navigate to high page numbers in large media libraries
- **Fix**: Implement cursor-based pagination using CreatedAt timestamp:
```csharp
.Where(f => request.CursorTimestamp == null || f.CreatedAt < request.CursorTimestamp)
.OrderByDescending(f => f.CreatedAt)
.Take(request.PageSize)
```

### **MEDIUM**: Missing Input Validation on File Variants
- **File**: src/Media/Media.Api/Application/GetMediaUrl.cs:21-24
- **Issue**: Variant validation only checks regex and path traversal but doesn't validate against allowed variant types
- **Impact**: Potential for requesting non-existent S3 keys, leading to 404s from S3
- **Fix**: Add validation against a whitelist of allowed variants:
```csharp
private static readonly string[] AllowedVariants = { "thumbnail-150", "thumbnail-300", "webp-150", "hls-master" };
RuleFor(x => x.Variant)
    .Must(v => v == null || AllowedVariants.Contains(v))
    .WithMessage("Invalid variant type.");
```

### **MEDIUM**: Resource Cleanup Gap
- **File**: src/Media/Media.Api/Application/CompleteMultipartUpload.cs:145-147
- **Issue**: Temp file deletion in finally block swallows all exceptions, potentially masking disk space issues
- **Impact**: Silent failures in cleanup could lead to disk space exhaustion over time
- **Fix**: Log cleanup failures but don't throw:
```csharp
try { File.Delete(tempPath); } 
catch (Exception ex) { logger.LogWarning(ex, "Failed to cleanup temp file {TempPath}", tempPath); }
```

### **MEDIUM**: Insufficient Error Context in Workers
- **File**: src/Media/Media.Api/Infrastructure/Workers/ProcessMediaConsumer.cs:31-38
- **Issue**: Idempotency check logs generic message without correlation to original upload request
- **Impact**: Difficult to troubleshoot processing issues or correlate with user reports
- **Fix**: Add more context to log messages:
```csharp
logger.LogInformation(
    "Skipping media processing for {MediaId} (owner: {OwnerId}): status is {Status}, expected Quarantined",
    msg.MediaId, msg.OwnerId, file?.Status.ToString() ?? "not found");
```

### **LOW**: Potential Integer Overflow in Part Count Calculation
- **File**: src/Media/Media.Api/Application/InitiateUpload.cs:89
- **Issue**: Part count calculation uses int cast without overflow protection
- **Impact**: Extremely large files (>56TB with 26MB parts) could cause integer overflow
- **Fix**: Add bounds checking:
```csharp
var partCountLong = (long)Math.Ceiling((double)request.Size / _uploadOpts.PartSizeBytes);
if (partCountLong > int.MaxValue)
    return Result.Failure<UploadResponse>(new Error("Media.FileTooLarge", "File exceeds maximum supported size."));
var partCount = (int)partCountLong;
```

### **LOW**: Missing Validation for Allowed Extensions vs MIME Types
- **File**: src/Media/Media.Api/Options/UploadOptions.cs:35-42
- **Issue**: Only validates MIME type but doesn't cross-check against file extension
- **Impact**: Users could upload .exe files with image/png MIME type (though file signature validation catches this)
- **Fix**: Add extension validation in InitiateUploadValidator:
```csharp
RuleFor(x => x.FileName)
    .Must((cmd, fileName) => IsValidExtensionForMimeType(fileName, cmd.MimeType))
    .WithMessage("File extension doesn't match MIME type.");
```

### **LOW**: Hard-coded Magic Numbers
- **File**: src/Media/Media.Api/Infrastructure/Processing/VideoProcessor.cs:67-72
- **Issue**: Quality tier configurations and bitrates are hard-coded in QualityTier definitions
- **Impact**: Requires code changes to adjust video encoding parameters
- **Fix**: Move to configuration:
```csharp
// In appsettings.json
"Transcode": {
  "QualityTiers": [
    { "Name": "1080p", "Height": 1080, "VideoBitrateKbps": 5000, "MinSourceHeight": 1080 }
  ]
}
```

## Summary

- **Total findings**: 9 (1 High, 4 Medium, 4 Low)
- **Top 3 most urgent items**:
  1. **Inefficient batch operations** causing performance issues with multiple file uploads
  2. **Race condition in file creation** potentially causing unique constraint violations
  3. **Pagination performance** degrading with large datasets
- **Overall service health rating**: **7/10** - Well-architected service with good security practices and proper event-driven patterns, but has some performance optimizations needed and edge case handling gaps. The Three-Phase Gateway pattern is correctly implemented, MassTransit outbox usage is proper, and security validations are comprehensive.

The service demonstrates good adherence to the platform's architectural laws and follows clean architecture principles. The main concerns are around performance optimization and edge case handling rather than fundamental design flaws.
